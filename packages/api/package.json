{
  "name": "@kanjo/api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Hono Worker (REST API + Workers Assets によるSPA配信)",
  "scripts": {
    "dev": "pnpm run migrate:local && wrangler dev",
    "migrate:local": "wrangler d1 migrations apply kanjo-db --local",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "pnpm run types && tsc --noEmit",
    "test": "vitest run",
    "test:coverage:account-login": "vitest run src/account-login.test.ts src/login-rate-limit.test.ts src/index.test.ts src/scheduled-maintenance-budget.test.ts --coverage.enabled --coverage.provider=v8 --coverage.reporter=text --coverage.include=src/auth.ts --coverage.include=src/users.ts --coverage.include=src/login-rate-limit.ts --coverage.include=src/routes/auth.ts --coverage.include=src/routes/admin-users.ts --coverage.include=src/index.ts --coverage.include=src/scheduled-maintenance-budget.ts --coverage.thresholds.statements=80 --coverage.thresholds.functions=80 --coverage.thresholds.lines=80"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.2",
    "@kanjo/core": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "fflate": "^0.8.2",
    "hono": "^4.13.5",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "miniflare": "5.20260911.0-alpha",
    "typescript": "^5.6.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.33.0"
  }
}
