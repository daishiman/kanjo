{
  "name": "@kanjo/api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Hono Worker (REST API + Workers Assets によるSPA配信)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "pnpm run types && tsc --noEmit",
    "test": "vitest run --testTimeout=60000 --hookTimeout=60000 --fileParallelism=false --maxWorkers=1"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.2",
    "@kanjo/core": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "fflate": "^0.8.2",
    "hono": "^4.6.14",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10",
    "miniflare": "5.20260820.0-alpha",
    "typescript": "^5.6.3",
    "vitest": "^3.2.6",
    "wrangler": "^4.33.0"
  }
}
