# task-progress (live 実行状態・派生ビュー)

> `project-task-status.py` 生成の派生ビュー。構造の正本は `task-graph.json`、状態の正本は build dir の `task-state.json`。手書き編集しない (再生成で上書き)。build 異常終了時は最後の 投影時点のスナップショットで stale の可能性がある (最新は再投影で得る)。

- 凡例: ✓=done / ▶=running / ✗=blocked / ☐=pending / ⏳=未処理の発見タスク (外ループ待ち)
- 完了率: **0%** (0/13)
- 状態内訳: done=0 / running=0 / blocked=0 / pending=13
- route-report 数: 0

## このタスクの目的と、導入で得られる価値

### 技術的な詳細 (エンジニア向け)
- **目的 (何をするか)**:
  - マトリックス画面を
  - 『どの月・どのカテゴリに支出が偏っているか』という問いに一画面で答える道具にする月×カテゴリ (または取引先) のヒートマップで偏りを見つけ
  - 気になるセルを選んで金額・前月比・前年同月比・含まれる取引・出典まで降り
  - 明細へ繋げるところまでを
  - 切替と選択を URL に保ったまま行える状態にする
- **到達状態 (Goal)**: /analysis/matrix が 06-matrix.png の全構成要素 (問いの見出しと最終更新・期間タブ・切替 3 群・単位と濃淡凡例・行ヘッダ固定のヒートマップ表と合計/平均の行列・選択中セルの詳細パネル・偏りが大きい 3 点・下部の選択中バー・空状態) をトークンと共通部品で描画し、集計 (スコープ / 軸 / モード / 合計と平均 / 濃淡階級 / 偏り上位 3 点 / セル内訳) が core の純関数 1 か所で算出されて GET /api/matrix と GET /api/matrix/cell が返し、画面側の再集計が無くなり、切替と選択が URL から復元でき、既存の matrix CSV・総収支・推移の同一期間の金額と一致し、サイドバー等の共通シェルは表記統一のみで既存テストが緑のままの状態。

## タスクの依存関係 (何が何に依存して進むか)
> 全 13 タスク・0 依存エッジ。各フェーズの詳細は下記チェックリスト、完全な関係は HTML レポートを参照。
- 起点タスク (依存なしで最初に着手可能): `SYS-MATRIX-P01`、`SYS-MATRIX-P02`、`SYS-MATRIX-P03`、`SYS-MATRIX-P04`、`SYS-MATRIX-P05`、`SYS-MATRIX-P06`、`SYS-MATRIX-P07`、`SYS-MATRIX-P08`、`SYS-MATRIX-P09`、`SYS-MATRIX-P10`、`SYS-MATRIX-P11`、`SYS-MATRIX-P12`、`SYS-MATRIX-P13`

## P01
> 🎯 何のため: 何を作るか — 要件と作業方針を固める
- ☐ `SYS-MATRIX-P01` 要件ベースライン確定と持ち越し4件の着手時整理

## P02
> 🎯 何のため: どう作るか — 構成・データ・依存を設計する
- ☐ `SYS-MATRIX-P02` core 集計関数・2 経路 API・URL 単一真実のワークストリーム設計決定記録

## P03
> 🎯 何のため: 設計を独立レビューで検証する
- ☐ `SYS-MATRIX-P03` 集計契約・API 契約・URL 契約の独立レビュー

## P04
> 🎯 何のため: 検証方法 (テスト) を先に設計する
- ☐ `SYS-MATRIX-P04` core 境界値・API 契約・DOM の失敗テスト先行作成 (持ち越し4件を契約テストで固定)

## P05
> 🎯 何のため: 各部品を実際に作る (実装)
- ☐ `SYS-MATRIX-P05` core 集計純関数・2 経路 API・マトリックス画面・表記統一の実装

## P06
> 🎯 何のため: 作った部品を動かして検証する
- ☐ `SYS-MATRIX-P06` 全テストと型検査と lint の実行記録

## P07
> 🎯 何のため: 合格ライン (受け入れ基準) を定める
- ☐ `SYS-MATRIX-P07` 受入基準 S1 から S5 の検証

## P08
> 🎯 何のため: 重複を整理し保守しやすくする
- ☐ `SYS-MATRIX-P08` 重複算出経路の除去と既存 matrix 利用箇所の整理

## P09
> 🎯 何のため: 全体の品質ゲートを通す
- ☐ `SYS-MATRIX-P09` アクセシビリティ・入力検証・応答上限・JS バンドル予算の保証確認

## P10
> 🎯 何のため: 最終レビューで仕上がりを確認する
- ☐ `SYS-MATRIX-P10` 独立最終レビュー

## P11
> 🎯 何のため: 検証した証拠を残す
- ☐ `SYS-MATRIX-P11` 再現可能な証跡索引の作成

## P12
> 🎯 何のため: 使い方・導入手順を文書化する
- ☐ `SYS-MATRIX-P12` 集計規則と表記統一の docs 最終同期

## P13
> 🎯 何のため: リリースしてよいか判定する
- ☐ `SYS-MATRIX-P13` 単一 PR での配信とクローズアウト

