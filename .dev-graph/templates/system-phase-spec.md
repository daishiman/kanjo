# System phase specification — compatibility stub

- Contract: `template-contract.json#/system_plan/external_templates/resources/system_phase`
- Compatibility purpose: 旧参照名を維持するためだけに残す。runtime では別 phase 文書を生成せず、各 phase を exact 13 executable tasks に統合する。
