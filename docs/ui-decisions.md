# UI改善の判断記録(2026-08-24 / feat/ui-refresh)

リファレンスHTMLの見た目の忠実再現は要件から外れ、ベストプラクティス優先に変更(発注者指示)。色・フォントのデザイントークンはHTML版継承のまま。計算ロジック(spec §8)とスナップショット期待値は不変。

| 判断 | 採用 | 理由 / 却下案 |
|---|---|---|
| モバイルナビ | 下部固定タブバー+ドロワー | 最頻タスクを1タップに。却下: ハンバーガーのみ(全操作が2タップになる)。※タブの構成は2026-08-30に更新(後述) |
| タブの選定 | 業務頻度順(毎日=概況/マトリクス、月次=仕分け/取込) | 残りは「メニュー」から開く。※画面数の増加に伴い2026-08-30に更新(後述) |
| ナビの頭文字アイコン | 廃止(テキストのみ) | 漢字1字アイコンは判読性が低く、jp-web-designで禁止。※図形アイコンの扱いを2026-08-30に更新(後述) |
| ヘッダーのアプリ名 | ホームへのリンク化 | 定石(現在地からの帰還手段)。h1はページ側のpage-titleが担う |
| 防衛ラインバッジ (FR-08) | 全幅で常時表示。狭幅は「見込」詳細のみ畳み、防衛線額+判定は残す | 完全非表示はFR-08違反。行を2段に折るのは高さを圧迫 |
| モーション | hover/pressed/ページ入場/ポップオーバーのみ、90〜200ms、reduced-motionで全停止 | 装飾アニメは追加しない |
| a11y | スキップリンク、:focus-visible統一(2px+offset)、Escape/外側クリックで閉じる、aria-expanded/haspopup、タップ領域確保 | |
| 表ヘッダー sticky | --header-h 変数で管理 | 53pxハードコードの分散を解消 |

## 決定の更新(2026-08-30 / feat-ui-navigation-cognitive-load)

上表は2026-08-24時点(全12画面)の記録である。画面が17に増え、`feat-ui-navigation-cognitive-load`でナビゲーションを作り直したため、次の3件を更新する。**上表の行は履歴として残し、以降はこの節を現行の判断とする。**

| 更新した判断 | 2026-08-24の記録 | 2026-08-30の決定 | 変更した理由 |
|---|---|---|---|
| モバイルタブの構成 | 概況・マトリクス・仕分け・取込・メニューの5要素 | `routeMetadata.ts`の`mobileLabel`を持つ5 route(概況・マトリクス・仕分け・領収書・取込)+メニューの6要素 | 確定申告の追加で領収書収集が月次の常用タスクになった。タブ本数はコードの`mobileLabel`が正本で、この文書は本数を二重に持たない |
| メニューへ送る画面数 | 残り7画面 | 残り12画面(17−5) | 画面が12→17に増えたため。「7画面」は12画面時代の数値で、更新されずに残っていた |
| ナビのアイコン | 廃止(テキストのみ) | **漢字1字・頭文字・絵文字のアイコンは引き続き禁止。意味を持つ図形アイコン(単色stroke SVG)は可**とし、17 routeすべてに付与する | 2026-08-24に禁止したのは「漢字1字アイコン」であって図形アイコン一般ではない。17画面の文字だけのナビでは走査が遅く、形の手掛かりが要る。判読性の懸念は、`lucide-static`由来の既製geometryを使い、可視labelを必ず残す(アイコンは`aria-hidden`の装飾扱い)ことで回避する |

### タブ5本+メニューという構成について

「タブは5個まで」の定石に対し、現行はタブ5+メニュー1=6要素で上限に接している。メニューは画面ではなく残り12画面への入口なので6本目のタブとしては数えない立場を取るが、**これ以上タブを増やす余地はない**。次に常用タスクが増えたら、タブを足すのではなく`mobileLabel`の入れ替えで対応する。

### この記録を古びさせないために

上の3件はいずれも「実装が変わったのに判断記録だけ取り残された」ことで生じた。ナビゲーションの構成を変えるときは、`docs/ui-navigation-guidelines.md`の契約とあわせてこの文書へ**決定の更新として追記**する(過去の行は消さない)。画面数・タブ本数のような数値は`routeMetadata.ts`を正本とし、文書側では極力持たない。

## 決定の更新(2026-08-30 / feat-ui-navigation-cognitive-load ウェーブ4)

上の節と同じfeatureの続き。**画面数を17から15へ減らした**ので、その判断を追記する。ここでも過去の行は消さない。

| 更新した判断 | これまで | 2026-08-30の決定 | 変更した理由 |
|---|---|---|---|
| 「見る」群の構成 | 増減マトリクス・支出トレンド・統計診断・サブスク分析が、それぞれ独立した画面としてサイドバーに並ぶ | 増減マトリクス・支出トレンド・統計診断を**支出分析(`/analysis/:tab`)の3タブへ統合**。サブスク分析は独立のまま | 前の3つは出口が同じ(見直す勘定科目を1つ選ぶ)で、利用者は3画面を行き来しながら1つの判断をしていた。サイドバーに3行並んでいること自体が「別々の仕事だ」という誤った合図になっていた |
| サブスク分析の扱い | 同上 | 統合しない | 集計の単位が違う(勘定科目ではなく支払先)。加えて支払先マスタの編集と見直し記録という**書き込み操作**を持ち、「見る」だけの画面ではない。同じタブ帯に並べると、タブの切り替えが「同じ対象の別の見方」でなくなる |
| モバイルタブの2番目 | マトリクス(`/matrix`) | 分析(`/analysis`) | 統合先へ差し替え。タブ本数は5のまま |
| 旧URL | `/matrix` `/trends` `/diagnosis` | 各タブへ`replace`でリダイレクト(`LEGACY_ROUTE_REDIRECTS`) | ブックマークされている可能性があり、トップへ落とすと行き先が分からなくなる |
| タブの状態 | — | **URLに持つ**(`/analysis/matrix`)。リンクで組み、`role="tab"`は手組みしない | ボタンで持つとリロードで先頭タブへ戻り、ブラウザの戻る/進むが切り口の移動を追えない。切り替えが実際のページ遷移なので、素のリンクのほうが支援技術に嘘がない |
| タブ内の読み込み | — | 表示中のタブだけを描画し、APIも1本だけ呼ぶ | 3本のクエリを同時に投げると、束ねた瞬間に遅くなる。対象期間は`PeriodProvider`が持つのでタブ間で保たれ、タブ内の表示切替(金額/前月比、事業/家計)は切り替えで初期値へ戻る |

### 統合しても高さは足りていない

サイドバー総高は **841px → 800px**(15行 × 36px + 群見出し4本)。ノートPCのブラウザ実効高 600〜700px には**まだ収まらない**。統合の成果は高さではなく、最上位の選択肢が「見る」群で8→6に減り、1つの判断のために画面を行き来する必要が消えたことにある。高さを根拠に「解決した」とは言わない。

### 説明文を消さないための約束

統合で消えた画面の`taskDetail`(色の凡例、判定基準など)は`ANALYSIS_TABS`へそのまま移し、タブごとの折りたたみ説明として表示している。用語ホバーの総数は`route-task-detail.test.tsx`が`APP_ROUTES + ANALYSIS_TABS`で数える。**画面を減らすときに説明文ごと捨てない**、というのがこの検査の意味である。

---

## 決定の更新(2026-08-30 / feat-transaction-split-readability)

明細の分割記帳で**科目が読めない・選べない**という指摘を受けた判断。ここでも過去の行は消さない。

| 更新した判断 | これまで | 2026-08-30の決定 | 変更した理由 |
|---|---|---|---|
| 分割エディタの科目パネル | `CategoryPicker`の`.cat-panel`を`position: absolute`で浮かせる(全画面共通) | **分割エディタの中だけ`position: static`にして、その場に開く** | 浮かせた要素は「一番近いスクロール祖先」で切られる。分割の表は`.scroll-x`(内側)と`.card.scroll-x.classify-table-card`(外側)に二重で囲われていたため、460pxのパネルが2〜3行ぶんの小窓に押し込まれ、その中をスクロールして選ぶ状態になっていた |
| 分割の表の囲い | `.scroll-x`(`overflow: auto` + `max-height`) | `.split-lines`(スクロール枠を作らない) | 分割エディタは元の明細表(`min-width: 1160px`)の中に展開される。横幅はすでに確保されており、内側でもう一度スクロール枠を作る必要がない。枠を作る代わりに、パネルが切れないほうを取った |
| 科目列の幅 | 内容なり(閉じているとき約150px) | 開く前から`480px`で固定確保 | 開いた瞬間に列が伸びると、隣の金額欄が動いて押し間違える。`.cat-detail { min-height: 72px }`(触れた科目の説明で高さを固定する)と同じ考え方を、縦だけでなく横へも適用した |

### 浮かせるのをやめた代わりに失ったもの

パネルを開くと**行の高さが伸び、下の行が下がる**。`.cat-picker`のコメントが元々警告していた挙動そのものである。分割エディタでは、下の行が動くことより「何を選んでいるか読めない」ことのほうが致命的だと判断して、この副作用を受け入れた。他の画面(仕分け一覧・設定)のピッカーは浮かせたままにしてある。

### この決定を古びさせないために

`packages/web/scripts/check-mobile-layout.mjs`の不変条件9に、**開いた分割パネルが分割エディタ内部の`overflow`で切られていないこと**を実描画で固定した。外側の明細一覧は意図したスクロール領域なので検出対象から外し、以前パネルを小窓にしていた内側だけを見る。将来ここへ`overflow`を足すと、実ブラウザ検査が落ちる。

---

## 決定の更新(2026-09-03 / feat-35-source-neutral-spending)

| 更新した判断 | 採用 | 理由 / 却下案 |
|---|---|---|
| 入力元と用途 | freee/MFのcanonicalは分離したまま、現在行から支出実態を読む投影を追加 | 入力元を用途の代理にすると、MF-only事業支出とfreee個人支出を誤る。保存モデルの統合は取消・undo・税務正本を壊すため却下 |
| 重複排除 | 日付・絶対金額・用途・厳格支払先が1対1で一致し、MF安定IDあり・非分割だけ自動照合 | 金額だけ、±日付、法人格除去などの曖昧一致は別取引を消す。候補は隠さず要確認へ残す |
| 代表画面 | 支出分析の先頭に「支出照合」を追加し、帳簿確定・未記帳・実質支出を同じ月別表で比較 | 原本別の合計画面を増やすと利用者が再計算する。既存削除操作は公私仕分け/データ取込へ一本化し、第二の削除UIは作らない |
| サブスク | 登録集計と候補の双方を照合後の実質支出から導出 | freee固定ではMF-only契約を落とし、単純合算では二重に数えるため |

---

## 決定の更新(2026-09-07 / ナビゲーション追補)

`feat-total-cashflow`の受入(SYS-TCF-P07)を実データで確かめようとしたとき、**トータル収支タブへ行けなかった**。原因は画面ではなく動線で、そこから確認ダイアログの欠陥も一緒に見つかった。判断の正本は`architecture/arch-ui-navigation-experience.md`のADR-UI-005〜007。

| 更新した判断 | これまで | 2026-09-07の決定 | 変更した理由 / 却下案 |
|---|---|---|---|
| 支出分析のタブへの行き方 | サイドバーは親「支出分析」1行だけ。タブは画面に入ってから選ぶ | **5タブをサイドバーの子行として常時展開する** | 束ねた画面は「出口が同じ」だが、行き先の名前まで消してよいという意味ではなかった。親の名前しか出ていないと、タブの存在が画面外にある。却下: 親クリックで開閉するdisclosure(畳んだ状態が既定になると同じ問題が残り、開閉状態の保存先という新しい判断も生む)、5タブを最上位routeへ昇格(最上位の選択肢が15→20に戻り、統合で減らした認知負荷を打ち消す) |
| タブ配下でのcurrent | 親「支出分析」1件をcurrentにする | **子行1件だけをcurrentにし、親には立てない** | 子行が並んだ以上、親子ともcurrentだと現在地が2件になる。親routeを完全一致にするADR-UI-001と同じ原則 |
| 破壊的操作の確認 | `window.confirm` | **アプリ内`<dialog>`(`ConfirmDialog` + `usePendingConfirm`)** | ブラウザの「このページでこれ以上ダイアログを表示しない」が効くと`window.confirm`は即`false`を返し、呼び出し側は抑止と拒否を区別できない。押しても無反応なボタンになり、理由も画面に出ない。却下: 確認をやめてundoを用意する案(削除済みデータの保持が要り、データ境界を変える) |

### 高さと引き換えに取ったもの

子行5本ぶん(約140px)サイドバーが高くなった。ノートPCの実効高に収める話は`specs/ui-navigation-cognitive-load.md`の未決事項に残っている。**「収まること」より「行き先が見えること」を優先した**という判断であり、タブが増えれば群の折りたたみを考える必要が出る。

### この決定を古びさせないために

- `navigation-ux.dom.test.tsx`が、各タブのhrefと「`/analysis/total-cashflow`で`[aria-current="page"]`が1件」を固定する。親にcurrentを戻すとここが落ちる。
- 確認ダイアログ側は「`confirm`が呼ばれない」だけでは足りない。確認せず素通りする実装も通ってしまうので、`findByRole('dialog')`・本文・確定ボタン名の三点と組で固定した。この3件が旧実装(確認なし)で落ちることは実測してある。

---

## 決定の更新(2026-09-08 / 税申告・証憑機能の廃止)

| 更新した判断 | これまで | 2026-09-08の決定 | 変更した理由 |
|---|---|---|---|
| ナビゲーションの対象 | 15 route + 支出分析5タブ | **13 route + 支出分析5タブ**。本数は引き続き`routeMetadata.ts`を正本とする | `/tax`と`/tax/receipts`を製品目的から外した。存在しない画面をガイドの固定数や受入へ残さない |
| モバイルの常設route | `mobileLabel`を持つ5 route | **`mobileLabel`を持つ4 route**(概況・分析・仕分け・取込)+メニュー | 領収書routeの廃止による帰結。代替タブは増やさず、最頻業務だけを残す |
| 廃止URL | — | `/tax`と`/tax/receipts`は特別なredirectを持たず、通常の未知routeと同じ入口へ戻す | 廃止機能への期待を残す転送先がなく、別機能へ誤誘導しないため |

固定数はこの判断時点の履歴であり、現行契約では`APP_ROUTES` / `ANALYSIS_TABS` /
`MOBILE_ROUTES`を直接数える。検索、サイドバー、モバイルメニューへ別の一覧を作らない。

---

## 決定の更新(2026-09-15 / 支出分析ハブ)

`/analysis`(タブ名なし)を、5つの分析のどれから見ればよいかを1画面で判断できる**ハブ**にした。判断の正本は`specs/spec-analysis-hub.md`と`architecture/analysis-hub-frontend.md`。ここでは、2026-08-30に決めた「表示中のタブだけAPIを呼ぶ」への**例外(制約C3)**を記録する。`architecture/analysis-hub-frontend.md`のC3が、この例外をここへ明記するよう求めている。

| 更新した判断 | これまで | 2026-09-15の決定 | 変更した理由 / 却下案 |
|---|---|---|---|
| `/analysis`の行き先 | 照合タブへ`replace`で転送 | **ハブを描く**。`/analysis/:tab`と`LEGACY_ROUTE_REDIRECTS`は従来どおり | 転送すると、どのタブから見るべきかを判断する場所が無い。綴りの違うタブ名は従来どおり既定タブへ寄せる |
| 選択中の分析の保持 | — | `?focus=<タブid>`で持ち、行選択は`replace`(履歴を積まない)。許可リスト外の値は既定(照合) | リロードやURL共有で同じ選択に戻れるようにする。履歴を積むと、戻るでハブの外へ出にくくなる |
| URLコピーの中身 | — | `origin + /analysis?focus=<id>`だけ。期間・金額は載せない | 期間は`PeriodProvider`(localStorage)が持つ。受け取った人は自分の期間で同じ分析を開く。URLに財務情報を残さない |
| **C3の例外: 集約API** | 表示していないタブのAPIは呼ばない | **ハブ画面とサイドバーの件数バッジだけが`GET /api/analysis/hub`を呼ぶ**。5タブ個別のAPIは引き続き表示中のタブだけが呼ぶ | ハブは5視点の要確認件数・変化・改善余地を見比べる画面で、5本を並列に呼ぶと2026-08-30に避けた遅さとD1読取りに戻る。集計はcoreの純関数で行い、APIは1本に束ねた。却下: ハブから5タブのAPIを並列に呼ぶ(C3の原則に正面から反し、読取りも5倍)、バッジを出さない(子行を開くまで要確認の有無が分からない) |
| 呼出しを1本に保つ仕組み | — | ハブとLayoutが**同じqueryKey**(`routeMetadata.ts`の`analysisHubQueryKey(期間key)`)を使う。Layoutは`enabled: !locked`でロック中は呼ばない | キーが分かれると、ハブ表示中にサイドバーとハブで同じAPIが2本飛ぶ。キーは遅延読み込みの`Analysis.tsx`ではなく、Layoutからも参照できる`routeMetadata.ts`に置く |
| 件数バッジの出し方 | — | 要確認が1件以上の視点(照合・総収支)の子行だけ。リンクの**外**の兄弟要素に置き、読み上げは「要確認 N 件」 | リンクの中に入れるとリンク名が「総収支2」になり、タブ名での読み上げが崩れる。0件でも出すと常に赤くなり、警告の意味が消える |
| 5タブの表示名 | 支出照合・トータル収支・増減マトリクス・支出トレンド・統計診断 | **照合・総収支・マトリクス・推移・診断** | サイドバー子行・タブ・現在地表示が`ANALYSIS_TABS[].label`を共有するので同時に変わる。支出分析以外の文言は変えない |

### 例外を広げないために

- 集約APIを呼んでよいのは**ハブ画面とサイドバーのバッジだけ**。タブ画面(`/analysis/:tab`)は自分のAPIだけを呼ぶ(`analysis-tabs.dom.test.tsx`「表示していないタブのAPIは呼ばない」)。別の画面が「ついでに」集約APIを使い始めたら、この例外の範囲を見直す。
- `staleTime`はアプリ共通値と同じ30秒。分類・分割・予算は`invalidateAnalysisDerived`、総収支の判定・freee除外は`invalidateAnalysisHub`を成功時に呼び、保存後は期間別の`['analysis-hub']`全てを古い状態にしない。

### この決定を古びさせないために

- `analysis-hub.dom.test.tsx`「ハブ表示中は集約 API 1 本だけを呼び、5 タブの API を呼ばない」が、集約APIの呼出し1回と5タブAPIの呼出し0回を固定する。queryKeyを分ける、ハブからタブAPIを呼ぶ、のどちらでも落ちる。
- 同ファイルの「サイドバーの要確認バッジ」2件が、件数の一致・0件で出さないこと・リンク名がタブ名のままであることを固定する。0件でも出す/全視点に出す/件数を固定、の3変異で落ちることは実測してある。
- `analysis-tabs.dom.test.tsx`「タブ名の無いURLはハブを出し、綴りの違うURLは既定のタブへ寄せる」「統合前のURLは行き先を失わない」がURLの入口を、`navigation-ux.dom.test.tsx`「タブを開いているときは、その子だけが現在地になる」が短縮名での現在地1件を固定する。

## 決定の更新(2026-09-18 / マトリックス画面)

`/analysis/matrix` を作り直した。判断の正本は `specs/spec-expense-matrix-screen.md` と `architecture/expense-matrix-*.md`、見取り図は [`matrix.md`](matrix.md)。ここでは、2026-09-15 に決めた**タブの表示名を変えたこと**と、濃淡(ヒートマップ)という新しい表現を入れたことを記録する。

| 更新した判断 | これまで | 2026-09-18の決定 | 変更した理由 / 却下案 |
|---|---|---|---|
| タブの表示名 | マトリクス(2026-09-15) | **マトリックス** | `routeMetadata.ts` の `label` と `journeyHint` が正本で、サイドバー子行・タブ・現在地表示が同時に変わる。画面内の見出しと表記が食い違っていた |
| 金額の大きさの見せ方 | 数値のみ | **7 階級の濃淡**。分母は表全体で共通、対象は本体セルだけ | 行ごとに分母を取り直すと月をまたいで比べられない。合計行・合計列を分母に混ぜると、総計が一桁大きいので本体セルが最下位階級へ潰れる |
| 濃淡を塗る範囲 | — | **金額モードだけ**。前月比・前年同月比では塗らない | 率に同じ分母は使えない。仕様 §3.3 が率モードの分母を定めていないので、決めていないものを描かない |
| 色だけに依存しないこと | — | セルに金額を常に表示し、凡例は階級の**下限を実際の判定と同じ配列から**引く | 濃淡の順序だけで判断させると色覚の異なる利用者が読めない。凡例を別に書くと判定とずれる |
| 「大きく動いた科目」の出どころ | 画面ごとに別の式(`financial-chart-model.ts` の `matrixMovers` ほか) | **core の `matrixSkewTop` 1 本**。偏りの定義は `analysis.ts` の `zScores` だけを使う | 同じ表から違う順位が出ていた。却下: 画面側に閾値を置く(定義が再び分かれる) |
| 未記帳月の扱い | 画面ごとに判断 | **`matrix-derived.ts` の `recordedIndexes` 1 か所**。合計・平均・比率・濃淡のすべてから除外し、セルは空欄 | 0 を混ぜると平均が下がり標準偏差が上がり、記帳済みの月がどれも平均より上に見える |

### この決定を古びさせないために

- `matrix-derived.test.ts` が同点解消の順序(スコア → 金額 → 月 → 行)、窓外の前年同月、未記帳月の除外を境界値で固定する。
- `heat-grid.dom.test.tsx` と `matrix-legend.dom.test.tsx` が、凡例の階級下限が実際の判定と同じ配列から来ていることを固定する。凡例だけを書き換えると落ちる。
- `heatScaleOf` は渡された値の min/max を取るだけで、どの行が合計かを知らない。**合計を分母から外すのは表を組み立てる側(`model.ts` の `body`)の責務**であり、この分担が崩れると濃淡が全部薄くなる形で現れる。

### まだ決めていないこと

セルを選んだときの詳細パネルと明細への遷移(受入 S2)、選択セルの URL 復元(受入 5)、行の分類を取引先へ切り替える軸(§2.2)は本サイクルで実装していない。仕様側の記述は緩めていないので、次に着手するときは `specs/spec-expense-matrix-screen.md` がそのまま使える。

## 決定の更新(2026-09-19 / 家計収支画面)

`/household` を作り直した。判断の正本は `specs/spec-household-cashflow-screen.md` と `architecture/household-cashflow-*.md`、実装判断の記録は [`household-screen/design-decisions.md`](household-screen/design-decisions.md)、集計の定義は [`data-schema.md`](data-schema.md) の「家計収支の集計」。

| 更新した判断 | これまで | 2026-09-19の決定 | 変更した理由 / 却下案 |
|---|---|---|---|
| ナビの表示名 | 累計収支 | **家計収支** | 画面が答える問いが「累計」ではなく「家計全体の収支がどう変わったか」になった。`routeMetadata.ts` の `label` / `task` / `journeyHint` を同時に変えた |
| 家計全体の定義 | 事業と個人を別々に並べる | **家計全体 = 事業 + 個人**(重複なし)。総収支画面の総合と一致させる | 2 つの画面で「全体」の数字が違うと、どちらを信じればよいか分からない。却下: 画面側で合算する(定義が 2 か所に分かれる) |
| 名義の表示名 | コード内の固定語(`OWNER_LABEL`) | **既定は 本人 / パートナー / 子ども / その他**。利用者が「名義ラベルを編集」ダイアログで 1〜20 文字に変えられ、家計・設定・明細・仕分けの全画面に同じ名前で出る | 家族構成は世帯ごとに違う。保存成功時に名義を表示する画面のキャッシュをまとめて無効化し、画面ごとに古い名前が残らないようにする |
| 画面状態の URL | 無し | **`seg`(グラフの対象: 既定 all は書かない)/ `month`(選択月)/ `cat`(開いているカテゴリ。`none` は閉じた状態)** | 再読み込みや共有で同じ画面に戻れるようにする。期間外の `month` は API が `invalid_month` を返し、画面が指定を外して期間の最終月へ戻す |
| 前年データが無い月・項目 | 0 として比べる | **「—」を表示し、増減も率も出さない**(BR-006) | 0 と比べると「前年より 100% 増えた」という誤った読みになる |
| 用語集 | `explainability` / `savingsRate` を収録 | **2 語を外した** | 作り直した画面のどこにも出ない語で、`check-glossary` が「辞書にあるが画面で使われない語」として落とす |
| 事業と個人の純収支 | — | **R4 の決定値(事業 +¥571,000 / 個人 +¥185,000)** | spec の画像の数値が自己矛盾していたため、計算で整合する値を正とした(design-decisions §3 R4) |

### この決定を古びさせないために

- `packages/core/test/household-summary-contract.test.ts` が spec のフィクスチャの計算値、前年欠損の null、振替の対推定、そして `data-schema.md` の 6 区分の表と `HOUSEHOLD_CATEGORY_MAP` の一致を固定する。表だけ・core だけを書き換えると落ちる。
- `packages/web/src/pages/household/household.dom.test.tsx` が URL の `seg` / `month` / `cat` の読み書きと、前年欠損の「—」を固定する。
- `packages/api/test/owner-labels.integration.test.ts` が名義ラベルの入力検証(空・21 文字・制御文字・重複・未知のキー)、20 文字ちょうどの保存、取込み中の 409 を固定する。
