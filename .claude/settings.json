{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/scripts/hooks/guard-real-data.sh\" --host claude",
            "if": "Bash(git *)",
            "timeout": 10,
            "statusMessage": "実データ混入をチェック中..."
          }
        ]
      }
    ]
  }
}
