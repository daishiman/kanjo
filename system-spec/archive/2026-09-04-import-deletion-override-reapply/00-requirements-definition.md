---
status: confirmed
category: requirements-definition
---

# 要件定義書 (上位概念)

> 本章は spec-state.json の requirements_foundation を正本とする、システム構築の憲法。
> 以降の各技術章は frontmatter の serves_goals でここ (ゴール) へトレース (anchor) する。
> 上位概念がブレなければ、仕様が整った後もブレない。

- 確定マーカー: `status: confirmed`

## U1 本質的目的 (essential_purpose)

freee・マネーフォワード側で元データが後から書き換えられても、利用者が『今ある取込データを、どう消して・どう上書きして・どう入れ直すか』を自分で選んで実行でき、かつ一度自分で直した科目・項目・公私区分といった手当てが取込のたびに失われず継続的に再適用されることで、毎回の入力の手間を極限まで下げながら、帳簿を正しい最新状態へ収束させられる状態を保つ。

## U2 背景 (background)

収支統合管理はfreeeの取引エクスポートとマネーフォワードME の収入・支出詳細/資産推移CSVを取り込んで帳簿を作る。しかし取込元は生きたサービスであり、後から科目名の変更・項目名の変更・明細の追加削除・金額修正が行われる。その結果、既に取り込んだ内容と再ダウンロードしたCSVの内容が食い違う。現在の実装は取込単位では『ファイルに含まれる月の既存データを消して置き換える』月単位の洗い替えだけを持ち(mfCommitStatements の deleteReplacement / assetsCommitStatements の deleteImported)、原本をR2から取込枠へ戻す再取込(POST /restore)も備える。一方で取込APIには削除系のエンドポイントが1本も無く(POST /imports・GET /imports・GET /imports/:id/original・POST /restore のみ)、『この取込をなかったことにする』『この期間を空にする』『全部消して入れ直す』手段が利用者に無い。さらに上書き時に既存と新規のどちらを採るかは実装が一律に決めており、利用者が明細単位・項目単位で判断する余地が無い。手動で入れた公私仕分けのoverride・分割(tx_splits)・現金の記帳(cash_entries)・証憑(attachments)は明細IDが一致する限り維持される設計だが、元データ側でIDが振り直されると黙って孤立する。取込履歴画面には『この取込をやり直す』しか操作が無く、削除も差分確認も存在しない。 さらに、利用者は取り込んだ明細に対して科目や項目名を自分で書き換えて運用している。同じ取引先からの入金・支払いは毎月繰り返し発生するため、取込のたびに同じ手当てを入力し直すことが大きな手間になっている。現状の手当ての受け皿は rules (keyword→公私区分)・overrides (tx_id→公私区分)・account_norm_map (raw科目→正規科目) の3つだが、overrides は tx_id に依存するため、マネーフォワード側でIDが振り直されると手当てが黙って孤立する。また『前回取り込んだときの原本値』をどこにも保持していないため、いま目の前にある値と新しいCSVの値が食い違ったときに、利用者が直したのか取込元が変わったのかを機械的に区別できず、どちらを採るべきかの判定ができない。利用者は将来的に、こうした選択を都度行わなくても自動で判定される状態を望んでいる。

## U3 ゴール (goals)

| ID | ゴール |
|---|---|
| G1 | 取込済みデータを、取込単位・期間単位・データ種別単位・全件のいずれの粒度でも、利用者が意図して削除できる状態にする |
| G2 | 同じ対象を取り込み直すとき、既存と新規が食い違う箇所を実行前に可視化し、上書き・保持・個別選択を利用者が決められる状態にする |
| G3 | 削除と上書きのどちらの経路でも、手動で入れた記録(公私仕分け・分割・現金の記帳・証憑)が意図せず失われず、失う場合は事前に件数で示される状態にする |
| G4 | 削除・上書きは不可逆操作であることを踏まえ、実行前の確認・実行後の取り消し・監査可能な記録を備え、誤操作から回復できる状態にする |
| G5 | 削除・上書き後も、月次集計・現行指紋(import_active_targets)・取込履歴・残高などの派生状態が実データと矛盾しない状態へ必ず収束する |
| G6 | 利用者が一度直した科目・項目・公私区分などの手当てを、同じ取引先・同じ性質の明細へ継続的に再適用し、取込のたびに入力し直す手間をなくす |
| G7 | 取込元の変更と利用者の手当てが衝突したとき、前回取込時の原本値を基準に『利用者が変えたのか取込元が変わったのか』を機械的に区別し、既定でどちらを採るかを自動判定する。判定できない箇所だけ利用者へ問う |

## U4 目標 (objectives)

| ID | 目標 | 測定基準 |
|---|---|---|
| O1 | 取込単位の取り消しを提供する | 取込履歴の各行から、その取込が書いた明細だけを取り消せ、取り消し後にその取込のimport_idを参照する行が0件になる |
| O2 | 期間単位・種別単位・全件の削除を提供する | 月または期間、freee仕訳/MF明細/資産推移/現金記帳の種別、および全件を指定して削除でき、指定範囲外の行が1件も減らない |
| O3 | 取込実行前に既存との差分を提示する | 追加・変更・削除・不変の件数と、変更については項目単位の新旧値を実行前に確認でき、確認せずに書き換えが起きない |
| O4 | 上書きポリシーを利用者が選べる | 同一キーの食い違いに対し『新規で上書き』『既存を保持』『1件ずつ選択』を選べ、選択結果どおりに反映される |
| O5 | 手動記録の巻き添えを事前に示し保全する | 削除・上書きの確認画面に、影響を受ける公私仕分け・分割・現金記帳・証憑の件数が表示され、保全可能なものは実行後も残る |
| O6 | 削除・上書きを取り消せる | 実行直後に取り消し操作があり、取り消し後の各テーブルの行数と内容が実行前と一致する |
| O7 | 派生状態の再収束を保証する | 削除・上書き後にmonthly_agg・import_active_targets・imports・balance_entriesが実データから再計算した値と一致する |
| O8 | 手当てを継続的に再適用する | 一度科目や公私区分を直した取引先の明細が、次回以降の取込で自動的に同じ扱いになり、同じ手当ての再入力が0件になる |
| O9 | 3点比較で変更の出どころを判別する | 前回取込原本値・現在値・新規原本値の3点から、利用者が触っていない項目は新規値、利用者が直した項目は現在値を既定で採り、両方が変わった箇所だけを利用者へ提示する |
| O10 | 利用者へ問う件数を逓減させる | 手当ての蓄積に応じて確信度が上がり、閾値以上は自動適用、閾値未満だけを提示するため、同種の取引について問われる件数が回を追って減る |
| O11 | 学習した決め事を利用者が確認・取り消せる | 自動適用された決め事の一覧を画面から確認でき、個別に取り消すと以後の自動適用が止まる |

## U5 成功基準 (success_criteria)

- 取込履歴から任意の取込を取り消せ、取り消し後にその取込由来の明細が0件になり、他の取込由来の明細が1件も減らない
- 月・期間・種別・全件を指定した削除ができ、指定範囲外のデータが変化しない
- 取込実行前に、追加/変更/削除/不変の件数と変更内容の新旧値を確認でき、確認を経ずに書き換えが起きない
- 同一明細の食い違いに対して上書き・保持・個別選択のいずれかを選べ、選択どおりに反映される
- 削除・上書きの確認時に、失われる可能性のある手動記録の件数が示され、示された件数以外は失われない
- 実行直後の取り消しにより、実行前の状態へ完全に戻せる
- 削除・上書き後に月次集計・現行指紋・取込履歴・残高が実データと矛盾しない
- 誰がいつ何をどれだけ削除・上書きしたかが記録として残り、後から確認できる
- 同じ取引先の明細について、一度直した科目・公私区分が次回以降の取込で自動的に同じ扱いになる
- 利用者が触っていない項目は新規CSVの値が採られ、利用者が直した項目は手当てが維持される
- 利用者の手当てと取込元の変更が同じ項目で衝突した箇所だけが提示され、それ以外は自動で決まる
- マネーフォワード側で明細IDが振り直されても、日付・金額・取引先などの安定した手掛かりで手当てが追随する
- 自動適用された決め事を一覧で確認でき、取り消せば以後適用されない

## U6 ステークホルダー (stakeholders)

- {'id': 'S1', 'role': '利用者 (個人事業の記帳担当)', 'need': '取込元で修正した内容を帳簿へ正しく反映したい。そのために古い取込を消したり入れ直したりする手段が要る。手で入れた仕分けや現金記帳を巻き添えで失いたくない'}
- {'id': 'S2', 'role': '開発兼運用者 (daishiman)', 'need': '削除・上書きが派生状態を壊さないこと。誤操作の問い合わせに対して何が起きたかを記録から追えること'}
- {'id': 'S3', 'role': '税務上の記録の正確性を担保する立場', 'need': '確定申告の根拠となるデータが、意図しない削除や上書きで欠落・改変されないこと。変更の履歴が残ること'}

## U7 スコープ (scope)

- **対象 (in)**: 取込単位(import_id)の取り消しと、その取込が書いた明細・派生状態の巻き戻し, 期間単位(月・期間指定)の削除, データ種別単位(freee仕訳/MF明細/資産推移/現金記帳)の削除, 全件削除(初期化), 取込実行前の差分プレビュー(追加/変更/削除/不変の件数と項目単位の新旧値), 上書きポリシーの選択(新規で上書き/既存を保持/1件ずつ選択), 削除・上書きが手動記録(公私仕分け・分割・現金記帳・証憑)へ与える影響の事前提示と保全, 削除・上書きの取り消し(undo), 削除・上書きの監査記録, 削除・上書き後の派生状態(monthly_agg/import_active_targets/imports/balance_entries)の再収束, 取込履歴画面への削除・差分確認・上書き選択のUI追加, 前回取込時の原本値の保持と、3点比較(前回原本・現在値・新規原本)による変更元の判別, 取引先単位の決め事(科目・項目・公私区分)の学習と、新規明細への自動適用, 自動適用の確信度と、利用者へ問う閾値の設計, 明細の同一性を取込元IDだけに頼らず、安定した手掛かり(日付・金額・取引先・口座)でも辿れるようにすること, 学習した決め事の確認・取り消しUI
- **対象外 (out)**: CSV/ZIPのパース仕様そのものの変更 (列の追加・形式判定ロジックの変更), freee/マネーフォワードのAPI連携による自動同期, 取込以外の経路で作られたデータ(手入力の予算・ルール定義など)の一括削除, 認証方式の変更, D1以外のストア(R2の原本オブジェクト)の保持期間ポリシーの変更, 明細データを外部サービスやLLMへ送信して学習させること (明細は外部送信しない既存方針を維持する), freee/マネーフォワード側のデータを本システムから書き換えること

## U8 制約 (constraints)

- 実行基盤は Cloudflare Workers + D1 + R2 で、D1には1リクエストあたりのクエリ本数上限があり、既存の query budget 設計 (D1_FREE_QUERY_LIMIT=50 未満) を壊さない
- 取込は import_writer_claims による利用者別の単一writer claim (TTL 15分) の配下で行われ、削除・上書きも同じ排他の下で実行する
- 重複判定は import_active_targets の現行指紋で行うため、削除はこの指紋の巻き戻しと必ず対で行う
- 明細内容や金額をログ・エラー応答へ含めない既存のセキュリティ方針を維持する
- 税務上の正はfreeeの記帳であり、本システムのデータ削除がfreee側へ波及してはならない
- 本番D1への破壊的操作であるため、削除・上書きは利用者の明示的な確認を経て実行する
- 既存の月単位洗い替え(mfCommitStatements/assetsCommitStatements)と再取込(POST /restore)の挙動を壊さない
- スキーマ変更が必要な場合は migrations/ 配下の連番SQLをappend-onlyで追加し、既存テーブルの破壊的再定義を避ける
- 学習した決め事はブラックボックスにせず、利用者が一覧で確認し個別に取り消せること
- 自動適用は既存の rules (keyword→公私区分) / overrides (tx_id→公私区分) / account_norm_map (raw科目→正規科目) と矛盾させず、優先順位を明示すること
- 明細データを外部へ送信せずローカル(D1)の情報だけで判定すること

## U9 具体的にやりたいこと (concrete_intents)

| ID | やりたいこと | 資するゴール |
|---|---|---|
| I1 | 取込履歴の各行に『この取込を取り消す』を設け、その取込が書いた明細と派生状態を巻き戻す | G1, G5 |
| I2 | 期間・種別・全件を指定して削除する経路を設ける | G1 |
| I3 | 取込実行前に既存データとの差分(追加/変更/削除/不変)を件数と項目単位の新旧値で提示する | G2 |
| I4 | 食い違いに対する上書きポリシー(新規で上書き/既存を保持/1件ずつ選択)を利用者に選ばせる | G2 |
| I5 | 削除・上書きで影響を受ける手動記録(公私仕分け・分割・現金記帳・証憑)の件数を確認画面へ出し、保全可能なものは残す | G3 |
| I6 | 削除・上書きの直後に取り消せる経路と、その保持期間を設ける | G4 |
| I7 | 削除・上書きの操作記録(いつ・何を・何件)を残し、後から確認できるようにする | G4 |
| I8 | 削除・上書き後にmonthly_agg・import_active_targets・imports・balance_entriesを実データから再収束させる | G5 |
| I9 | 明細の同一性を取込元IDだけに頼らず、日付・金額・取引先・口座などの安定した手掛かりでも辿れるようにし、IDが振り直されても手当てが追随するようにする | G6, G3 |
| I10 | 前回取込時の原本値を明細ごとに保持し、前回原本・現在値・新規原本の3点比較で変更の出どころを機械的に区別する | G7, G2 |
| I11 | 取引先単位の決め事(科目・項目・公私区分)を学習して蓄積し、新規明細へ自動適用する | G6 |
| I12 | 自動適用に確信度を持たせ、閾値以上は自動適用し、閾値未満だけを利用者へ問う。利用者の回答を確信度へ反映して次回以降の問合せを減らす | G6, G7 |
| I13 | 自動適用された決め事を一覧で確認でき、個別に取り消せる画面を設ける | G6, G4 |

## 意思決定支援 (decisions)

| ID | 論点 | 状態 | 選択肢 (費用・適合・注意点) | AI推奨 | ユーザー決定 | 資するゴール |
|---|---|---|---|---|---|---|
| D1-undo-snapshot-store | 削除・上書きの取り消し(undo)のために、直前状態をどこへ退避するか | confirmed | opt-d1-table:D1 に退避テーブルを新設する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '既存 kanjo-db 内へテーブルを足すだけで、追加のサービス契約も課金枠も発生しない。構築費は migration 1本と掃除ジョブ1本の実装工数、運用費は掃除ジョブの監視のみ。移行・撤退は標準SQLiteテーブルとしてそのまま持ち出せる。無料枠超過後は D1 の従量課金。'} / free=D1 無料プラン: 1データベースあたり最大 500 MB、アカウント総ストレージ 5 GB、データベース数 1〜10、rows read 500万/日、rows written 10万/日、Worker 1回の呼び出しあたりクエリ 50 本、Time Travel (時点復旧) の保持 7 日。 / fit=G4(確認・undo・監査記録)に直接適合する。退避行を SQL で直接引き当てられるため、明細単位・取込単位・期間単位のどの粒度でも巻き戻し対象を選び出せる。G5(派生状態の再収束)についても、退避行から monthly_agg を再計算できる。 / pros=巻き戻し対象を SQL で直接絞り込める, 保存先が増えず、既存のアクセス制御と掃除経路をそのまま使える, トランザクション境界を削除本体と揃えられる / cons=明細を行として持つためストレージが行数に比例して増える, 保持期間の掃除を自前で実装する必要がある, Free では単一データベースが 500 MB 上限のため、退避行の保持期間を長く取りすぎると容量を圧迫する / risks=大量削除時に退避行数が D1 の 1リクエストあたりクエリ本数上限 (既存設計は 50 未満) を圧迫するため、分割実行が必須になる, 削除1行につき退避1行の書込が加わるため rows written を二重に消費し、Free の 10万 rows written/日 に到達しやすくなる / lock-in=Cloudflare D1 依存が増えるが、既に kanjo-db が正本であり新たなロックインは生じない。退避テーブルは標準 SQLite 構文で、移行時も普通のテーブルとして持ち出せる。 / ops=保持期間経過後の掃除ジョブを1本持つ。既存 migrations/ の連番 append-only 方針に沿ってテーブルを足すだけで、運用対象のサービスは増えない。 / evidence=https://developers.cloudflare.com/d1/platform/pricing/, https://developers.cloudflare.com/d1/platform/limits/<br>opt-r2-snapshot:R2 へスナップショットを JSON/CSV で退避する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '既存 kanjo-files (R2) を使い無料枠内は月額0円。構築費はスナップショット生成・展開処理とD1側削除記録との突合実装、運用費はライフサイクル設定と孤児オブジェクトの監視。撤退時は S3 互換のため持ち出し可能だが保存先が2箇所に分かれる分だけ移行対象が増える。'} / free=R2 無料枠: ストレージ 10 GB-月、Class A 操作 100万/月、Class B 操作 1000万/月、下り転送無料。オブジェクトは最大 5 TiB、単発アップロード 5 GiB、同一キーへの同時書込は毎秒1回。 / fit=G4 の巻き戻しは可能だが、スナップショット全体を読み出して展開する必要があり、明細単位の部分巻き戻しには不向きである。 / pros=行数が増えてもストレージ単価が安い, 取込原本と同じ置き場で保持期間の考え方を揃えられる / cons=部分巻き戻しにスナップショット全体の読み出しと展開が必要, D1 と R2 の間で削除と退避の原子性が取れない / risks=退避オブジェクトの書込成功と D1 側の削除の間で失敗すると、退避のない削除または孤児オブジェクトが残る, 同一キーへの書込は毎秒1回までのため、退避オブジェクトのキーを削除操作ごとに一意にする必要がある / lock-in=R2 依存。S3 互換 API のため移行余地はある。 / ops=オブジェクトのライフサイクル設定で掃除できる一方、D1 側の削除記録との突合を運用で持つ必要がある。 / evidence=https://developers.cloudflare.com/r2/pricing/, https://developers.cloudflare.com/r2/platform/limits/ | opt-d1-table — undo は『どの粒度でも直前状態へ戻せる』ことが要件であり、明細単位の部分巻き戻しが SQL で直接できる D1 退避テーブルが目的に最も適合する。削除本体と同じデータベース内で完結するため、退避と削除の原子性も取れる。 (注意: 大量削除時に退避行の書込が D1 Free の 1リクエストあたりクエリ本数上限 50 本を超えるため、既存の planMultipartImportQueries と同じ考え方で分割実行を必ず設計に含めること, D1 Free は単一データベースが 500 MB 上限であり、退避行の保持期間を長く取ると本体データを圧迫する。保持期間を有限にし、期限切れを確実に掃除する経路を持たせること, 削除1行につき退避1行の書込が加わり rows written を二重に消費するため、Free の 10万 rows written/日 に対する1日あたりの削除規模の見積りを実装時に確認すること, D1 の Time Travel は Free で 7 日しか遡れず、しかもデータベース単位の復元であるため undo の代替にならない。退避テーブルによる自前の巻き戻しが必要である; confidence=high; checked=2026-08-30T00:00:00+09:00) | opt-d1-table @ 2026-08-30T00:00:00+09:00 | G4, G5 |
| D2-overwrite-default-policy | 再取込で既存データと新しい取込元データが食い違うとき、既定でどう振る舞うか | confirmed | opt-show-diff-then-choose:差分を見せてから選ばせる (自動で決まる分は黙って通す) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '追加サービスなし。構築費は差分算出処理と差分提示画面の実装工数、運用費は不要。撤退費なし。'} / free=追加の外部サービスを使わないため無料枠の消費は D1 の読み書きのみ (5 GB ストレージ、500万 rows read/日、10万 rows written/日)。 / fit=G2(差分可視化と上書きポリシー選択)に直接適合する。G3(手動記録の巻き添え防止)についても、利用者が触った箇所を勝手に上書きしないため適合する。 / pros=利用者が触った手当てを勝手に失わせない, 何が変わるかを事前に確認できる, 自動で決まる分を黙って通すことで確認件数を最小化できる / cons=差分算出と提示の実装が必要, 衝突が多いとき利用者の確認操作が残る / risks=差分算出が遅いと取込全体の待ち時間が伸びるため、D1 のクエリ本数上限内でまとめ読みする設計が要る / lock-in=なし。アプリ内の処理と画面だけで完結する。 / ops=運用対象のサービスは増えない。差分算出規則の変更時に既存の base 値との整合を確認する必要がある。 / evidence=https://git-scm.com/docs/git-merge, https://www.sqlite.org/lang_upsert.html<br>opt-source-always-wins:常に取込元で上書きする / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '追加サービスなし。構築費は最小。運用費・撤退費なし。'} / free=同上 (D1 の読み書きのみ)。 / fit=G2 の『取込元の最新へ収束させる』側にだけ適合し、差分の可視化と選択という要件を満たさない。 / pros=実装が最も単純, 帳簿が常に取込元と一致する / cons=利用者が直した科目・公私区分・名義が毎回消える, G6(手当ての継続的な再適用)と正面から矛盾する / risks=確定申告向けに手で整えた分類が再取込のたびに失われ、作業のやり直しが発生する / lock-in=なし。 / ops=低いが、失われた手当ての復旧問合せが運用負荷になる。 / evidence=https://git-scm.com/docs/git-merge<br>opt-local-always-wins:常に手元の値を維持する (取込元の変更を無視する) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '追加サービスなし。構築費は最小。運用費・撤退費なし。'} / free=同上 (D1 の読み書きのみ)。 / fit=G3 には適合するが、G2 の『取込元の変更を帳簿へ反映して最新へ収束させる』を満たさない。 / pros=手当てが絶対に消えない, 実装が単純 / cons=取込元側の訂正 (金額修正・取引先名の修正) が永久に反映されない, 利用者が触っていない箇所まで古いままになる / risks=取込元の訂正を取り込めず、帳簿が実態から乖離する / lock-in=なし。 / ops=低いが、乖離の発見と手動修正が運用負荷になる。 / evidence=https://git-scm.com/docs/git-merge | opt-show-diff-then-choose — 取込元の訂正を反映しつつ利用者の手当てを守るには、両者を一律にどちらか優先とするのではなく、変更の出どころで場合分けし、機械的に決まらない箇所だけを提示するしかない。差分提示は G2 と G3 を同時に満たす唯一の選択肢である。 (注意: 差分を全件提示すると確認が苦役になるため、D3 の3点比較で機械的に決まる分は提示せず件数の要約に留めること, 差分算出のために前回取込原本値 (base) を保持していることが前提であり、base が無い既存明細については初回取込時に base を埋める移行処理が要る, 差分提示の既定は手当ての維持とし、無操作で確定した場合に手当てが消えない設計にすること; confidence=high; checked=2026-08-30T00:00:00+09:00) | opt-show-diff-then-choose @ 2026-08-30T00:00:00+09:00 | G2, G3 |
| D3-auto-classification-method | 利用者の手当てを継続的に再適用し、先月と今月で内容が食い違うときにどちらが正しいかを判定するために、どの方式を採るか。将来的に選択なしで判定できる状態へどう近づけるか | confirmed | opt-3way-vendor-memory:3点比較 + 取引先単位の決め事の学習 (確信度つき) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '追加サービスなし。構築費は tx_edits への base 列追加・安定同一性キー算出・vendor_memory テーブルと確信度更新処理・決め事一覧画面の実装工数。運用費は決め事の棚卸しのみ。撤退時は標準SQLiteテーブルとして持ち出せる。'} / free=D1 無料プラン内で完結 (1データベース 500 MB、rows read 500万/日、rows written 10万/日、Worker 1回あたりクエリ 50 本)。vendor_memory は取引先数に比例するため数百行規模に収まる。 / fit=G7 について、base(前回取込原本) / current(利用者の手当て) / incoming(新規原本) の3点から変更の出どころを機械的に区別でき、片方しか変わっていない箇所は自動で決まる。G6 について、確定した手当てを取引先単位で蓄積して新規明細へ自動適用するため、同じ設定の再入力がなくなる。確信度が上がるほど問われる件数が減り、選択なしの判定へ漸近する。 / pros=判定根拠が真理値表として説明でき、なぜその値になったかを後から辿れる, 利用者が触っていない箇所は自動で取込元へ追従し、触った箇所だけが守られる, 明細IDが振り直されても安定同一性キーで手当てが追随する, 学習が進むほど確認件数が単調に減る / cons=tx_edits への base 列追加と既存明細への base 埋め移行が必要, vendor_memory の確信度算出規則を設計・検証する必要がある / risks=安定同一性キーの正規化が緩いと別明細を誤って同一視して手当てが混線する, 誤学習した決め事が自動適用されると、取消するまで誤分類が広がる / lock-in=なし。SQLite の標準機能と自前ロジックだけで完結し、外部サービス依存を増やさない。 / ops=長期間当たらない vendor_key の棚卸しと、誤学習時の巻き戻し導線の維持。運用対象のサービスは増えない。 / evidence=https://git-scm.com/docs/git-merge, https://www.sqlite.org/lang_upsert.html, https://developers.cloudflare.com/d1/platform/limits/<br>opt-rules-only:既存のキーワード規則 (rules) の拡充だけで対応する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '追加サービスなし。構築費はほぼゼロ (既存 rules をそのまま使う)。運用費は利用者自身が規則を登録・保守する手間。'} / free=D1 無料プラン内で完結 (1データベース 500 MB、rows read 500万/日、rows written 10万/日、Worker 1回あたりクエリ 50 本)。vendor_memory は取引先数に比例するため数百行規模に収まる。 / fit=G6 について、規則が当たる範囲では再入力を減らせるが、規則を利用者が手で登録し続ける必要があり『入力の手間を極限まで下げる』を満たさない。G7 については前回取込原本値を持たないため、変更の出どころを区別できず判定できない。 / pros=実装がほぼ不要, 規則の内容が完全に利用者の管理下にある / cons=規則の登録・保守を利用者が手で続ける必要がある, 取込元の変更と手当ての衝突を判定できない, 将来的な無選択判定へ近づかない / risks=規則が増えるほど競合と順序依存が読めなくなり、意図しない分類が起きる / lock-in=なし。 / ops=低いが、利用者側の規則保守負担が残り続ける。 / evidence=https://www.sqlite.org/lang_upsert.html<br>opt-external-ml:推論モデル (Workers AI 等) へ明細を渡して分類させる / cost={'category': 'low-cost', 'amount': 1, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'Cloudflare Workers AI は $0.011 / 1,000 Neurons で、無料枠は1日あたり 10,000 Neurons。明細件数に比例して消費し、超過すると操作がエラーになるため Workers Paid が要る。構築費は推論連携とプロンプト設計、運用費は精度監視とモデル更新追従、撤退費は分類履歴の移行。'} / free=Workers AI 無料枠: 1日あたり 10,000 Neurons (毎日 00:00 UTC にリセット)。超過すると以降の操作はエラーになる。 / fit=分類の自動化という点では G6 に部分適合するが、G7 の『変更の出どころを機械的に区別する』は3点比較の情報がなければ推論では代替できない。判定根拠が確率的で、なぜその値になったかを説明できない。 / pros=未知の取引先にも初回から分類候補を出せる可能性がある / cons=明細の外部送信が必要で既存方針に反する, 判定根拠を説明できない, 件数に比例して課金が発生する, 同じ入力でも結果が揺れうる / risks=家計・事業の明細が外部へ渡る, 推論の誤りが静かに帳簿へ混入し、根拠がないため発見が遅れる, 無料枠 10,000 Neurons/日 を超えると操作がエラーになるため、取込処理が推論の可用性と残枠に依存する / lock-in=推論API 提供者への依存が生じ、モデル変更で挙動が変わる。 / ops=精度監視・モデル更新追従・費用監視が新たな運用対象になる。 / evidence=https://developers.cloudflare.com/workers-ai/platform/pricing/, https://git-scm.com/docs/git-merge | opt-3way-vendor-memory — 『先月と今月で差分があるとき、どちらが正しいか』という問いは、前回取込時の原本値という共通の基準がなければ原理的に答えられない。base / current / incoming の3点が揃えば、利用者が変えたのか取込元が変わったのかが確定的に分かり、片方しか変わっていない箇所は自動で決まる。そのうえで確定した手当てを取引先単位で蓄積すれば、同じ取引先の次回以降は自動適用され、確信度が上がるほど問われる件数が減って選択なしの判定へ漸近する。外部送信も追加費用もなく、判定根拠が真理値表として説明できる点で他案に優る。 (注意: 安定同一性キーの正規化が緩いと別明細を誤って同一視して手当てが混線するため、既存 packages/core/src/fingerprint.ts の正規化方針に揃え、指紋版数を持たせて算出規則の変更に追従できるようにすること, 既存明細には base 値がないため、初回取込時に現在の取込値を base として埋める移行処理が要る。この移行の前後で手当ての扱いが変わらないことを確認すること, 確信度の閾値は最初から自動適用を広げすぎず、まず候補提示から始めて、誤学習の取消率を見ながら段階的に緩めること, 自動適用された明細には決め事由来である印を残し、決め事へ辿れるようにしないと、誤学習の発見が遅れる, 判定は Worker 1回の呼び出しあたりクエリ 50 本 (D1 Free) の中で行う必要があるため、明細ごとに個別クエリを発行せず、取込単位でまとめ読みし UPSERT で書き戻す設計にすること; confidence=high; checked=2026-08-30T00:00:00+09:00) | opt-3way-vendor-memory @ 2026-08-30T00:00:00+09:00 | G6, G7 |
| D5-conflict-attribute-granularity | 現行の明細単位の真偽1つで表された衝突(core/classify.ts の conflict)を属性単位へ作り替えるにあたり、明細単位の表現を導出値として残すか、呼出側をすべて属性単位へ直すか | confirmed | opt-d5-full-attribute:resolveTx の戻り値を属性単位の3分岐へ作り替え、呼出側をすべて属性単位へ直す / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'リポジトリ内の型と呼出側の改修だけで完結し、追加のサービス契約も保存先も発生しない。構築費は resolveTx の戻り値型変更と、これを読む routes/classify.ts・Classify.tsx・既存テストの追随。運用費はゼロ。撤退は型を戻す改修に等しい。'} / free=追加の外部サービスを使わないため、無料枠の消費は現行の取込経路と変わらない。Cloudflare D1 無料プランの Worker 1呼び出しあたりクエリ 50 本という上限に対しても、判定はメモリ上で行うためクエリ本数を増やさない。 / fit=G2(食い違う箇所を実行前に可視化し、上書き・保持・個別選択を利用者が決められる)に最も強く適合する。『どの属性が食い違ったか』が戻り値そのものであるため、画面は属性ごとに上書き・保持を選ばせられる。G7(利用者が変えたのか取込元が変わったのかを機械的に区別する)についても、DR-10 の3分岐が属性ごとに1対1で表れるため、判定と表示のあいだで情報が落ちない。 / pros=衝突の所在が属性として表現され、利用者へ問う単位と判定の単位が一致する, 明細単位の真偽という第二の表現を持たないため、真実の源が1つに保たれる, DR-10 の3分岐が属性ごとにそのまま型へ現れ、分岐の取りこぼしを型検査で捕まえられる / cons=resolveTx の戻り値型が変わるため、routes/classify.ts・Classify.tsx・既存テストが一斉に波及する, 改修範囲が一度に広がり、途中状態で取込経路を壊さないための順序立てが要る, 既存テストの期待値をすべて属性単位へ書き直す必要がある / risks=波及範囲の見積りを誤ると、取込経路が動かない期間が生じる, 属性の追加が将来起きたとき、3分岐の網羅を型で強制していないと分岐漏れが入る, 画面が属性ごとの選択を提示するため、衝突が多い取込で利用者の判断量が増える / lock-in=低い。すべてリポジトリ内の型と実装であり、外部サービスへの依存を作らない。 / ops=低い。運用中に監視・調整する対象を増やさない。 / evidence=https://git-scm.com/docs/git-merge, https://www.sqlite.org/lang_upsert.html<br>opt-d5-derived-compat:属性単位の3分岐を追加しつつ、明細単位の conflict を導出値として残す / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '同じくリポジトリ内で完結し追加費用は発生しない。構築費は属性単位フィールドの追加のみで、既存の呼出側は当面そのまま動く。運用費はゼロだが、二重表現が残るぶん将来の改修時に両方を確認する手間が継続する。'} / free=追加の外部サービスを使わないため、無料枠の消費は現行の取込経路と変わらない。 / fit=G2 の可視化は属性単位フィールドを使えば実現できる。ただし明細単位の conflict が併存するため、画面と API のどちらを真とするかが実装ごとに揺れうる。G7 の機械的な区別そのものは満たせる。 / pros=既存の呼出側とテストがそのまま動き、改修を段階的に進められる, 取込経路が動かない期間を作らずに済む, 属性単位への移行を、画面から順に部分的に行える / cons=衝突の表現が明細単位と属性単位の二重になり、どちらが真かを実装ごとに判断する余地が残る, 導出規則(属性のどれか1つでも衝突なら明細も衝突)を別途定義し、維持し続ける必要がある, 移行を完了させる期限がないと、二重表現が恒久化する / risks=明細単位と属性単位で判定が食い違う経路が生まれ、画面と API で異なる衝突件数が出る, 導出規則の変更が明細単位の挙動を静かに変え、既存テストが捕まえられない / lock-in=低い。ただし二重表現が残るぶん、後から opt-d5-full-attribute へ寄せる改修が別途必要になる。 / ops=低い。ただし改修のたびに二系統の表現を確認する手間が継続する。 / evidence=https://git-scm.com/docs/git-merge, https://www.sqlite.org/lang_upsert.html<br>opt-d5-keep-line-level:現行の明細単位の真偽のまま据え置き、衝突は明細ごとに丸ごと問う / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '改修が発生しないため構築費も運用費もゼロ。ただし利用者が衝突のたびに明細を丸ごと見比べる負担が残り続ける。'} / free=追加の外部サービスを使わないため、無料枠の消費は現行の取込経路と変わらない。 / fit=G2 の『食い違う箇所を可視化する』を満たさない。衝突したことは分かるが、どの属性が食い違ったかを示せないため、利用者は明細全体を見比べて自分で差分を探すことになる。G7 の『判定できない箇所だけ問う』についても、問う単位が明細全体になり範囲が過大になる。 / pros=改修が不要で、既存のテストと呼出側に一切波及しない, 取込経路の挙動が変わらないため、退行の危険がない / cons=どの属性が食い違ったかを利用者へ示せない, 利用者が明細全体を見比べて差分を自分で探すことになる, 属性ごとに『上書き』『保持』を選ぶ操作が成立しない / risks=衝突が多い取込で利用者が判断を諦め、一括で片側へ倒す運用が常態化する, 利用者の手当てが、確認されないまま取込元の値で上書きされる / lock-in=なし。ただし要望そのものが未達のまま残る。 / ops=なし。 / evidence=https://git-scm.com/docs/git-merge | opt-d5-full-attribute — 利用者の要望は『コンフリクトが発生した場合はユーザーに尋ねてこれを整えるフロー』であり、尋ねる単位が明細全体では要望を満たさない。したがって opt-d5-keep-line-level は候補として成立しない。残る2案の差は、明細単位の conflict を導出値として残すかどうかに尽きる。導出値として残す opt-d5-derived-compat は改修を段階化できる利点があるが、衝突の表現が二重になり、画面と API で異なる衝突件数が出る経路を作る。本機能は『何件が食い違っているか』を実行前に利用者へ示して不可逆操作の可否を判断させるものであり、件数の食い違いは判断の根拠そのものを壊す。真実の源を1つに保つ利点が、段階化の利点を上回る。加えて DR-10 の3分岐を属性ごとに型へ表すことで、分岐の取りこぼしを型検査で捕まえられる。波及する routes/classify.ts・Classify.tsx・既存テストは、改修の順序を立てて一括で追随させる。 (注意: resolveTx の戻り値型の変更は、routes/classify.ts・Classify.tsx・既存テストへ同時に波及する。取込経路が動かない期間を作らないよう、型変更と呼出側の追随を同一の変更単位で行うこと。, 属性が将来増えたときに3分岐の網羅が崩れないよう、属性の集合と3分岐を型で表し、分岐漏れが型検査で落ちる形にすること。, 属性ごとの選択を画面へ出すと、衝突が多い取込では利用者の判断量が増える。全属性に対する一括指定の手段を併せて用意すること。, 既存テストの期待値を属性単位へ書き直す際、旧実装のままでは緑にならないことを確認すること。緑にするために契約を緩めないこと。; confidence=高。改修範囲は既知のファイルに限られ、判定の意味論は DR-10 で既に確定している。残る不確かさは既存テストの書き直し量の見積りに留まる。; checked=2026-09-02T00:00:00Z) | opt-d5-full-attribute @ 2026-09-02T00:00:00Z | G2, G7 |
| D6-base-backfill-migration | 前回取込時の原本値(base)を持たない既存明細について、いつ・どの経路で base を埋め、その移行の前後で利用者の手当ての扱いが変わらないことをどう確かめるか | confirmed | opt-d6-lazy-seed:再取込の実行時に、base を持たない明細だけ『その取込を始める直前の現行値』を base として埋めてから3点比較へ入る / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '埋める対象がその取込で実際に突き合わせる明細に限られるため、書込量が取込1回の明細数を超えない。構築費は取込経路への埋め込み1箇所とその試験、運用費はゼロ。撤退は埋め込みを外すだけで、既に埋まった base はそのまま有効な値として残る。'} / free=Cloudflare D1 無料プラン: 1データベース最大 500 MB、アカウント総ストレージ 5 GB、rows read 500万/日、rows written 10万/日、Worker 1呼び出しあたりクエリ 50 本、Time Travel 保持 7 日。本方式の書込は取込1回あたりの明細数(通常幅 5,000 行)に収まり、rows written 10万/日 に対して十分な余裕がある。 / fit=G7(前回取込時の原本値を基準に、利用者が変えたのか取込元が変わったのかを機械的に区別する)へ直接適合する。base を『取込直前の現行値』とすることで、移行前の明細は DR-10 の分岐1(current と base が一致=利用者は触っていない)ではなく、利用者の手当てを含んだ値が base になる。結果として初回だけ分岐2(incoming 採用)へ倒れて手当てが失われる事故を構造的に防ぐ。G3(手動で入れた記録が意図せず失われない)も同じ理由で満たす。 / pros=移行のための一括処理を持たず、既存データの規模に依存しない, 埋める値が『その取込を始める直前の現行値』であるため、利用者の手当てを base 側へ取り込める, 対象がその取込で突き合わせる明細に限られ、書込量が予測可能である / cons=再取込されない明細には base が埋まらないまま残る, 取込経路の中に移行処理が混ざるため、取込1回あたりの所要時間がわずかに増える, 『いつ移行が完了したか』という時点が存在せず、移行の進捗を件数でしか語れない / risks=埋め込みを3点比較より後に置くと、初回だけ分岐2へ倒れる事故がそのまま起きる。順序を取り違えると本決定の目的が失われる, 埋め込みと本体更新を同じ書込単位に入れないと、途中で失敗したとき base だけが進んだ状態が残る, 取込1回あたりの書込が明細数ぶん増えるため、極端に大きな取込では 1呼び出しあたりのクエリ本数上限へ近づく / lock-in=低い。埋め込みを外せば元の挙動へ戻り、既に埋まった base は有効な値として残る。 / ops=低い。常設のジョブを増やさず、監視対象も増えない。 / evidence=https://developers.cloudflare.com/d1/platform/limits/, https://www.sqlite.org/lang_upsert.html<br>opt-d6-batch-migration:migration で既存明細の全件へ base を一括生成し、移行完了の時点を明示する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '追加のサービスは不要だが、既存明細の全件を書き換えるため一時的に大きな書込が発生する。構築費は migration 1本と分割実行の実装、運用費は実行時の監視のみ。撤退には埋めた base を消す逆 migration が要る。'} / free=Cloudflare D1 無料プランの rows written は 10万/日 であり、既存明細の総数がこれを超える場合は日をまたいだ分割実行が必要になる。Worker 1呼び出しあたりのクエリ 50 本という上限に対しても、必ず分割実行が前提となる。 / fit=G7 を満たす。加えて『移行が完了した時点』が明確になるため、以後は base の有無で場合分けする実装を持たずに済む。G3 についても、埋める値を現行値とすれば手当てを base 側へ取り込める。 / pros=移行完了の時点が明確で、以後は base の有無による場合分けが不要になる, 再取込されない明細にも base が行き渡る, 移行の進捗と完了を件数で確認できる / cons=既存明細の総数によっては rows written 10万/日 を超え、日をまたいだ分割実行が要る, 分割実行の途中で失敗したときの再開位置の管理が要る, 本番データの全件を書き換えるため、実行前の明示確認と退避が要る / risks=全件書換の途中で失敗し、base が埋まった明細と埋まっていない明細が混在した状態が残る, 移行時点の現行値を base とするため、移行前に取込元が変わっていた場合その差が base へ焼き付く, 本番 D1 に対する大規模な書込であり、実行手順を誤ると回復に Time Travel の 7 日枠を使うことになる / lock-in=低い。ただし撤退には逆 migration が要る。 / ops=中。分割実行の進捗管理と、実行前の明示確認・退避の手順が要る。 / evidence=https://developers.cloudflare.com/d1/platform/limits/, https://developers.cloudflare.com/d1/platform/pricing/<br>opt-d6-user-wins-when-missing:base を埋めず、base を持たない明細では常に利用者側の現行値を採る / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '書込が一切発生せず、判定の分岐を1つ足すだけで済む。構築費は最小、運用費はゼロ。ただし base が永久に埋まらないため、この分岐が恒久的に残る。'} / free=書込を発生させないため、Cloudflare D1 無料プランのいずれの枠にも影響しない。 / fit=G3(手当てが意図せず失われない)は満たす。しかし G7 の『取込元が変わったのかを機械的に区別する』を満たさない。base が無いままでは、取込元の正当な更新も利用者の手当てと区別できず、一律に無視されることになる。 / pros=実装が最小で、書込が一切発生しない, 移行前の明細で利用者の手当てが失われることが構造的にありえない / cons=取込元の正当な更新が、base を持たない明細では永久に反映されない, base が埋まらないため、この分岐が恒久的に残る, 『情報が更新されていたらそれを反映させる』という利用者要望の中心が、既存明細に対して未達のまま残る / risks=取込元で金額や日付が訂正されても既存明細へ反映されず、帳簿が取込元と食い違い続ける, 食い違いが利用者に見えないまま蓄積し、後から差分の起点を特定できなくなる / lock-in=なし。ただし要望の中心が未達のまま残る。 / ops=なし。 / evidence=https://git-scm.com/docs/git-merge | opt-d6-lazy-seed — 利用者要望の中心は『ファイルが同じでも情報が更新されている可能性があるので、その更新された情報を今回のデータに反映させること』であり、取込元の更新を反映しない opt-d6-user-wins-when-missing は要望の中心を落とすため採れない。残る2案の差は、移行を一括で行うか取込のたびに行うかである。opt-d6-batch-migration は移行完了の時点が明確になる利点があるが、本番 D1 の全件書換であり、rows written 10万/日 の枠に対して日をまたいだ分割実行と再開位置の管理を要する。対して opt-d6-lazy-seed は、埋める対象がその取込で実際に突き合わせる明細に限られるため、書込量が取込1回の明細数(通常幅 5,000 行)を超えず、既存データの規模に依存しない。本決定が防ぎたい事故は『移行前の明細が初回だけ DR-10 の分岐2(incoming 採用)へ倒れて手当てが失われる』ことであり、これは埋める時点を3点比較より前に固定すれば構造的に防げる。移行完了の時点が存在しない弱点は、base を持たない明細の残件数を取込画面と監査記録に出すことで補う。埋める値は『その取込を始める直前の現行値』とし、埋め込みと本体更新は同じ書込単位に入れる。移行の前後で手当ての扱いが変わらないことは、base を持たない明細と持つ明細の双方に対して DR-10 の3分岐がそれぞれ期待どおりに倒れることを、件数を固定した試験で確かめる。 (注意: 埋め込みは必ず3点比較より前に置くこと。後に置くと初回だけ分岐2へ倒れる事故がそのまま起き、本決定の目的が失われる。, 埋め込みと本体更新を同じ書込単位に入れること。分けると途中失敗時に base だけが進んだ状態が残る。, base を持たない明細の残件数を取込画面と監査記録へ出すこと。移行完了の時点が存在しないため、残件数が唯一の進捗指標になる。, 移行の前後で手当ての扱いが変わらないことを、件数を固定した試験で確かめること。旧実装のままでは落ちる試験にすること。; confidence=高。書込量が取込1回の明細数に収まることは D1 の公表上限に対して十分な余裕があり、埋め込み位置と書込単位という2つの制約さえ守れば目的の事故は構造的に防げる。; checked=2026-09-02T00:00:00Z) | opt-d6-lazy-seed @ 2026-09-02T00:00:00Z | G3, G7 |
| D7-undo-retention-window | 削除・上書きの取り消し(undo)のために退避した行を、どれだけの期間・どれだけの量まで保持し、どの基準で掃除するか | confirmed | opt-d7-30d-capped:保持期間 30 日と容量上限の二段構えとし、いずれか先に到達した側で古い世代から掃除する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '退避先は既存 kanjo-db 内のテーブルであり、追加のサービス契約も課金枠も発生しない。構築費は掃除の判定と分割削除の実装、運用費は退避行の使用量を確認する監視のみ。撤退は保持期間の定数を短くするだけで済む。'} / free=Cloudflare D1 無料プラン: 1データベース最大 500 MB、アカウント総ストレージ 5 GB、rows read 500万/日、rows written 10万/日、Worker 1呼び出しあたりクエリ 50 本、Time Travel 保持 7 日。本方式は退避行の総量が 500 MB の一定割合を超えた時点で古い世代から掃除するため、単一データベースの上限に対して余裕を残したまま運用できる。 / fit=G4(実行後の取り消しと監査可能な記録を備え、誤操作から回復できる)へ直接適合する。誤って削除したことに利用者が気づくまでの猶予として、月次で帳簿を見返す運用に対し1か月を確保できる。G5(派生状態が実データと矛盾しない状態へ収束する)についても、退避行から月次集計と現行指紋を再計算できる期間が同じだけ確保される。 / pros=利用者が月次で帳簿を見返す運用に対し、気づくまでの猶予を確保できる, 容量上限を併せ持つため、大量削除が続いても単一データベースの 500 MB 上限へ到達しない, 掃除の基準が期間と容量の2つだけで、利用者へ『いつまで戻せるか』をそのまま画面に出せる / cons=容量上限に先に到達した場合、30 日未満で戻せなくなる世代が生じる, 掃除の分割実行を自前で実装する必要がある, 保持期間が Time Travel の 7 日より長いため、D1 の時点復旧では代替できず退避テーブルが唯一の回復手段になる / risks=容量上限による前倒しの掃除が起きたことを利用者へ示さないと、画面に出した期限と実際に戻せる期限が食い違う, 掃除の分割実行を怠ると、1呼び出しあたりのクエリ 50 本という上限を大量削除時に超える, 削除1行につき退避1行の書込が加わるため、rows written 10万/日 を二重に消費する / lock-in=低い。退避先は標準的な SQLite テーブルであり、そのまま持ち出せる。保持期間は定数であり運用で変えられる。 / ops=中。退避行の使用量の監視と、掃除ジョブの実行結果の確認が要る。 / evidence=https://developers.cloudflare.com/d1/platform/limits/, https://developers.cloudflare.com/d1/platform/pricing/<br>opt-d7-7d-timetravel-aligned:保持期間を 7 日とし、D1 の時点復旧が遡れる範囲と揃える / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '退避行の滞留が短いためストレージ消費が最小に収まる。構築費は掃除の判定のみ、運用費はほぼゼロ。撤退は保持期間の定数変更で済む。'} / free=Cloudflare D1 無料プランの Time Travel は 7 日ぶんの時点復旧を保持する。保持期間をこれに揃えることで、退避テーブルで戻せる範囲と D1 自身が遡れる範囲が一致する。1データベース 500 MB の上限に対しても、滞留が短いぶん余裕が大きい。 / fit=G4 を満たすが、猶予は 7 日に留まる。月次で帳簿を見返す運用では、誤削除に気づく前に退避行が消えている場合がある。G5 の再計算についても同じ期間に限られる。 / pros=退避行の滞留が短く、500 MB 上限に対する余裕が最も大きい, 退避テーブルで戻せる範囲と D1 の時点復旧が遡れる範囲が一致し、回復手段の説明が単純になる, 容量上限による前倒しの掃除がほぼ起きず、画面に出した期限と実際が食い違いにくい / cons=月次で帳簿を見返す運用では、誤削除に気づく前に退避行が消えている場合がある, 月末にまとめて確認する利用者にとって、実質的に取り消しが使えない期間が生じる / risks=誤削除に気づいた時点で退避行が既に消えており、回復手段がなくなる, 回復不能な事例が続くと、利用者が削除機能そのものを使わなくなる / lock-in=低い。保持期間は定数であり運用で変えられる。 / ops=低い。滞留が短く、容量の監視の重要度が下がる。 / evidence=https://developers.cloudflare.com/d1/platform/limits/<br>opt-d7-unbounded:保持期間を設けず、退避行を消さずに残し続ける / cost={'category': 'unknown', 'amount': None, 'currency': 'JPY', 'billing_period': 'month', 'tco': '退避行が削除操作の累計に比例して無制限に増えるため、単一データベースが 500 MB 上限へ到達する時期を見積れない。到達後は有料プランへの移行か退避先の外出しが必要になり、費用が事前に定まらない。'} / free=Cloudflare D1 無料プランの1データベース 500 MB という上限に対して、退避行が無制限に増えるため、いずれ上限へ到達する。到達時点でデータベースへの書込が行えなくなる。 / fit=G4 の『取り消しできる』は最大限に満たすが、DR-8(undo の保持期間は有限で、期限は画面に出す)に正面から反する。期限が存在しないため画面に出す期限もない。 / pros=いつでも任意の時点まで戻せる, 掃除の実装が不要になる / cons=退避行が削除操作の累計に比例して無制限に増える, 1データベース 500 MB の上限へ到達した時点で、データベースへの書込そのものが行えなくなる, DR-8 が要求する『画面に出す期限』が存在しない / risks=容量上限への到達で取込・削除の双方が停止し、復旧に有料プランへの移行を強いられる, 削除したはずの明細が無期限に残ることが、利用者の意図と食い違う / lock-in=高い。容量上限へ到達した後は、有料プランへの移行か退避先の外出しのいずれかを選ばざるを得ない。 / ops=低いように見えて高い。掃除は不要だが、容量の枯渇を常時見張る必要がある。 / evidence=https://developers.cloudflare.com/d1/platform/limits/, https://developers.cloudflare.com/r2/pricing/ | opt-d7-30d-capped — DR-8 は保持期間を有限と定めているため opt-d7-unbounded は不変条件に反し、候補として成立しない。残る2案の差は猶予の長さである。本機能が回復させたいのは『誤って取込を丸ごと消した』という不可逆操作であり、家計・事業の帳簿は月次で見返す運用が前提であることから、気づくまでの猶予は1か月を確保する必要がある。7 日では月末にまとめて確認する利用者に対して実質的に取り消しが使えない。一方で 30 日の滞留は退避行の量を増やすため、期間だけでは 500 MB の上限に対する保証がない。したがって期間と容量の二段構えとし、いずれか先に到達した側で古い世代から掃除する。具体値は次のとおりとする。保持期間 30 日。容量上限は退避テーブルの総量が 300 MB(単一データベース上限 500 MB の 60%)に達した時点とし、到達したら 30 日未満であっても古い世代から掃除する。掃除は1回の呼び出しあたりのクエリ本数が 50 本未満に収まるよう分割実行する。画面には『いつまで戻せるか』を世代ごとに表示し、容量上限による前倒しの掃除が起きた場合はその事実も併せて示す。1日あたりの削除規模については、削除1行につき退避1行の書込が加わり rows written 10万/日 を二重に消費することから、1日あたりの削除対象を 5万行以内に収める前提で運用を始める。 (注意: 容量上限による前倒しの掃除が起きたら、その事実を利用者へ示すこと。示さないと画面に出した期限と実際に戻せる期限が食い違う。, 掃除は1回の呼び出しあたりのクエリ本数が 50 本未満に収まるよう分割実行すること。大量削除時にまとめて消すと上限を超える。, 保持期間 30 日は Time Travel の 7 日より長いため、退避テーブルが唯一の回復手段になる期間が 23 日ある。退避テーブル自体の欠損に対する回復手段は別に考えること。, 300 MB という容量上限と 5万行/日 という削除規模は運用開始時点の出発点であり、実際の退避行の増え方を見て見直すことを前提とする。; confidence=中。30 日という猶予は月次で見返す運用から導けており確度が高いが、300 MB と 5万行/日 という具体値は実データのない段階の見積りであり、運用開始後の調整を前提とする。; checked=2026-09-02T00:00:00Z) | opt-d7-30d-capped @ 2026-09-02T00:00:00Z | G4, G5 |
| D4-vendor-memory-confidence | 取引先単位の決め事(vendor_memory)を自動適用してよいかを、どの式・どの初期閾値で判定し、どういう基準で段階的に緩めるか | confirmed | opt-d4-ratio-threshold:一致率と最小件数の固定式で判定し、取消率を見て閾値を段階的に上下させる / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'confidence は vendor_memory 行が既に持つ hit_count と disagree_count から算術で求めるため、追加のサービスも追加のテーブルも要らない。構築費は式1本と閾値定数の実装、運用費は取消率を集計する既存の監査記録からの読み出しのみ。撤退は閾値定数を上げて自動適用を止めるだけで済む。'} / free=Cloudflare D1 無料プラン: 1データベース最大 500 MB、アカウント総ストレージ 5 GB、rows read 500万/日、rows written 10万/日、Worker 1呼び出しあたりクエリ 50 本、Time Travel 保持 7 日。本方式は vendor_memory 行の読み書きだけで完結し、いずれの枠も新たに圧迫しない。 / fit=G6(手当ての継続再適用)に直接適合する。hit_count が閾値未満の決め事は自動適用せず候補提示に留めるため、学習が浅い段階で誤った手当てを広げない。G7(自動判定と、判定できない箇所だけ問う)についても、confidence が閾値未満のものだけを利用者へ問う形へ落とせるため、O10『利用者へ問う件数を逓減させる』を hit_count の増加として数値で測れる。 / pros=式が単純で、なぜ自動適用されたかを利用者へそのまま説明できる, hit_count と disagree_count だけで決まるため、既存の vendor_memory 行以外に状態を持たない, 閾値を定数として外に出せるため、緩和・引締めを実装変更なしに運用で回せる / cons=試行回数が少ない取引先で一致率が過大評価される(1件中1件一致で 1.0 になる), 取引先ごとの明細数の偏りを式が吸収しないため、最小件数の下限を別に置く必要がある, 閾値の初期値そのものは実データがない段階では根拠が薄く、運用開始後の調整が前提になる / risks=最小件数の下限を低く置くと、偶然の一致で自動適用が広がり利用者の取消が増える, 取消率の集計対象期間を短く取ると、季節性のある取引先で閾値が振動する, 閾値を運用で緩めた記録を残さないと、なぜ自動適用範囲が変わったかを後から追えなくなる / lock-in=低い。判定式と閾値定数がリポジトリ内に閉じており、外部サービスへの依存を作らない。方式を差し替える場合も vendor_memory 行はそのまま使える。 / ops=低い。取消率の定期確認と閾値定数の調整だけで、常設のジョブや監視対象を増やさない。 / evidence=https://developers.cloudflare.com/d1/platform/limits/, https://developers.cloudflare.com/d1/platform/pricing/<br>opt-d4-wilson-lower-bound:一致率の Wilson 下側信頼限界を confidence とし、試行回数の少なさを式自身に吸収させる / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '算出は同じ hit_count と disagree_count から求まる閉じた式で、追加のサービス契約も保存先も発生しない。構築費は式の実装とその境界値テスト、運用費は取消率の確認のみ。撤退時は confidence の算出関数だけを差し替える。'} / free=Cloudflare D1 無料プラン: 1データベース最大 500 MB、アカウント総ストレージ 5 GB、rows read 500万/日、rows written 10万/日、Worker 1呼び出しあたりクエリ 50 本、Time Travel 保持 7 日。本方式も vendor_memory 行の読み書きだけで完結する。 / fit=G6 に適合し、かつ試行回数が少ない取引先の confidence が自動的に低く出るため、最小件数の下限を別途置かなくても学習の浅い決め事が自動適用へ回らない。G7 の『判定できない箇所だけ問う』を、閾値1本だけで表現できる。 / pros=試行回数の少なさが confidence の低さとして式の中で自動的に表れる, 最小件数の下限という第二の閾値を持たずに済み、調整対象が閾値1本になる, 件数が増えるにつれ単純一致率へ滑らかに収束するため、閾値の意味が運用中に変わらない / cons=式が利用者への説明に向かず、なぜ自動適用されなかったかを画面上の言葉に落としにくい, 境界値テストの手当てが単純一致率より重い, 信頼水準という追加のパラメータが増え、閾値と併せて2つの数を決める必要が残る / risks=式の意味が運用者に伝わらないまま閾値だけ動かされ、意図しない自動適用範囲になる, 浮動小数点の扱いを取り違えると境界付近で判定が揺れ、同じ入力で自動適用の有無が変わる / lock-in=低い。外部サービスへの依存はなく、算出関数の差し替えで方式を戻せる。 / ops=低いが、閾値の意味を運用者へ説明する手間が opt-d4-ratio-threshold より重い。 / evidence=https://developers.cloudflare.com/d1/platform/limits/, https://www.sqlite.org/lang_upsert.html<br>opt-d4-workers-ai-judge:Workers AI の推論で明細と決め事の合致を判定させる / cost={'category': 'unknown', 'amount': None, 'currency': 'JPY', 'billing_period': 'month', 'tco': 'Workers AI は無料枠を超えると Neuron 単位の従量課金になり、取込のたびに明細数へ比例した推論費が積み上がる。構築費はプロンプトと評価の実装、運用費は推論費と出力揺れの監視で、撤退時は判定経路の全面差し替えが要る。'} / free=Cloudflare Workers AI は1日あたりの無料 Neuron 枠を持ち、超過分は Neuron 単位の従量課金となる。取込1回あたりの明細数に比例して消費するため、通常幅 5,000 行の取込では無料枠を早期に使い切る見込みである。 / fit=G6 の再適用そのものは実現しうるが、同じ入力に対して同じ判定が返る保証がないため、G7 の『機械的に区別する』という要求と噛み合わない。自動適用の根拠を後から再現できない。 / pros=取引先名の表記ゆれなど、式では拾いにくい類似性を扱える可能性がある, 決め事の記述を自然文のまま扱える / cons=同じ入力に対する判定の再現性が保証されず、監査記録として成立しない, 取込のたびに明細数へ比例した推論費が発生する, 判定に要する時間が取込全体の所要時間へ直接乗る / risks=明細内容が推論経路へ渡ることで、既存の外部送信禁止の制約に違反する, 無料枠を超えた時点で取込が費用面で継続不能になる, 判定根拠を利用者へ説明できず、誤適用時の回復手順が定まらない / lock-in=高い。判定がモデルの挙動に依存し、モデル更新で自動適用の結果が変わる。撤退には判定経路の作り直しが要る。 / ops=高い。推論費の監視、出力揺れの検知、モデル更新時の再評価が常時必要になる。 / evidence=https://developers.cloudflare.com/workers-ai/platform/pricing/ | opt-d4-ratio-threshold — O10『利用者へ問う件数を逓減させる』に測定基準を与えることが本決定の要点であり、そのためには confidence の値そのものより『なぜ自動適用したか／しなかったか』を利用者と運用者の双方が同じ言葉で追える式であることが優先される。一致率と最小件数の組は、画面上で『この取引先では過去 N 件中 M 件で同じ手当てをしています』とそのまま提示でき、監査記録にも同じ数が残る。試行回数の少なさを式に吸収できない弱点は最小件数の下限で補え、その下限自体も利用者へ説明できる数である。opt-d4-wilson-lower-bound は統計的には優れるが、自動適用の根拠を利用者へ説明する経路が失われる代償が、閾値が1本で済む利点を上回る。opt-d4-workers-ai-judge は明細の外部送信禁止と DR-14 に反するため、費用以前に候補として成立しない。具体値は次のとおりとする。confidence = hit_count / (hit_count + disagree_count)。自動適用の条件は hit_count >= 3 かつ confidence >= 0.80。直近30日の自動適用に対する利用者取消率が 5% を超えたら閾値を 0.05 上げ、取消率 1% 未満が30日続いたら 0.05 下げる。閾値の可動域は 0.70 以上 0.95 以下とし、上下させた事実と時刻を監査記録へ残す。 (注意: 最初から自動適用を広げず、hit_count が 3 に満たない決め事は自動適用せず候補提示に留めること。D3 の caveat と同じ方針を数値として引き継ぐ。, 閾値を上下させた事実・時刻・その時点の取消率を監査記録へ残すこと。残さないと自動適用範囲が変わった理由を後から追えない。, 取消率の集計対象は自動適用された明細に限り、利用者が自分で入れた手当てを取り消した場合を混ぜないこと。混ぜると閾値が誤って引き上がる。, 0.80 と 3 件という初期値は実データのない段階での出発点であり、運用開始後に取消率を見て見直すことを前提とする。; confidence=中。式と閾値の可動域の設計は確度が高いが、初期値 0.80 / 3 件そのものは実データのない段階の出発点であり、運用開始後の調整を前提とする。; checked=2026-09-02T00:00:00Z) | opt-d4-ratio-threshold @ 2026-09-02T00:00:00Z | G6, G7 |
| D8-audit-log-retention | 削除・上書きの監査記録そのものを、どれだけの期間・どの粒度まで保持し、どの基準で掃除するか (undo 退避行の保持は D7 で確定済み。ここで扱うのは何をいつ誰がどう変えたかを後から辿るための記録本体) | confirmed | opt-d8-two-tier:操作ヘッダを 400 日、判定明細スナップショットを 90 日とする二層保持にする / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '記録先は既存 kanjo-db 内のテーブルであり、追加のサービス契約も課金枠も発生しない。構築費は層ごとに異なる保持期間を持つ掃除の実装、運用費は行数と使用量の確認のみ。撤退は保持期間の定数を短くするだけで済む。', 'quantification': '操作ヘッダは1行あたり概ね 300 バイト (操作種別・対象範囲・件数・実行時刻・結果)、判定明細は1行あたり概ね 2 キロバイト (属性ごとの before/after と採用根拠) を見込む。月 3000 明細の利用で、ヘッダ 400 日ぶんが概ね 3.6 万行 11 MB、明細 90 日ぶんが概ね 0.9 万行 18 MB、合計およそ 29 MB となり、単一データベース上限 500 MB の 6 パーセントに収まる。'} / free=Cloudflare D1 無料プラン: 1データベース最大 500 MB、アカウント総ストレージ 5 GB、rows read 500万/日、rows written 10万/日、Worker 1呼び出しあたりクエリ 50 本、Time Travel 保持 7 日。本方式は重い側 (判定明細) の滞留を 90 日で区切るため、総量の増え方が線形に頭打ちになり 500 MB 上限に対して余裕を残せる。 / fit=G4(監査可能な記録を備え、誤操作から回復できる)へ直接適合する。確定申告や年次の見返しのように1年を越えて振り返る場面で必要になるのは『いつ何を何件消したか』という操作の事実であり、これはヘッダ側に載る。判定の根拠まで遡る必要が生じるのは異常に気づいた直後が大半で、90 日あれば足りる。G5(派生状態が実データと矛盾しない状態へ収束する)については、収束の検算に使う件数と対象範囲がヘッダ側に残るため 400 日ぶん追える。 / pros=年単位の振り返りで必要になる操作の事実を 400 日保てる一方、重い判定明細は 90 日で頭打ちになり容量が線形に膨らまない, 削除したはずの明細内容が監査記録の側に長期間残らないため、利用者の削除の意図と記録の保持が食い違わない, 掃除は層ごとに独立しており、D7 と同じ分割実行の考え方をそのまま流用できる / cons=保持期間の異なるテーブルが2つになり、掃除の実装と説明が1本のときより増える, 90 日を越えてから判定根拠を問われた場合、ヘッダの事実までしか答えられない, ヘッダと明細の境界をどちらへ寄せるかの判断が、記録項目を足すたびに発生する / risks=ヘッダ側へ明細内容や金額を載せてしまうと、90 日で消すはずの情報が 400 日残る, 層ごとの掃除を別々に実装するため、片方だけ掃除が止まっても総量の監視が効いていないと気づけない, 400 日の滞留は Time Travel の 7 日を大きく越えるため、監査記録テーブル自体の欠損は時点復旧では戻せない / lock-in=低い。標準的な SQLite テーブル2つであり、そのまま持ち出せる。保持期間はいずれも定数であり運用で変えられる。 / ops=中。2つの掃除ジョブの実行結果と、ヘッダ・明細それぞれの行数の確認が要る。 / evidence=https://developers.cloudflare.com/d1/platform/limits/, https://developers.cloudflare.com/d1/platform/pricing/<br>opt-d8-align-d7:監査記録も D7 と同じ 30 日・容量上限 300 MB の規則に載せる / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '退避行と同じ掃除経路へ相乗りするため、構築費は最小。運用費もひとつの規則の監視で済む。撤退は定数変更のみ。', 'quantification': '30 日ぶんであれば、月 3000 明細の利用でヘッダ・明細を合わせても概ね 2 MB 程度に留まる。'} / free=Cloudflare D1 無料プランの 1データベース 500 MB 上限に対し、30 日の滞留であれば消費は最小に留まる。掃除経路が D7 と共通のため、1呼び出しあたりクエリ 50 本の制約への対処も1箇所で済む。 / fit=G4 の『監査可能な記録』を 30 日の範囲でのみ満たす。前年の同月に何を取り込み何を消したかを確定申告の時期に振り返る、といった年単位の利用場面では記録が既に消えており、監査記録として機能しない。 / pros=保持規則が D7 と共通の1本で済み、実装と利用者への説明が最も単純になる, 容量消費が最小で、500 MB 上限に対する余裕が最も大きい, 掃除の分割実行を1箇所だけ正しく作ればよい / cons=30 日を越えると『なぜこの明細がこう変わったか』も『いつ何を消したか』も一切遡れない, 年単位の振り返り (確定申告・前年同月比) で監査記録として役に立たない, G4 が求める監査可能性の期間を、容量の都合だけで決めることになる / risks=誤った取込を数か月後に発見した際、原因を辿る手段が残っていない, 監査記録が短命であることを利用者が把握しないまま運用し、必要になった時点で初めて不在に気づく / lock-in=低い。定数変更で延ばせる。 / ops=低い。監視対象がひとつの規則に集約される。 / evidence=https://developers.cloudflare.com/d1/platform/limits/<br>opt-d8-row-capped:期間を設けず行数上限 20 万行とし、超過分を古い順に削除する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'month', 'tco': '上限行数が固定のため容量の最大値が事前に定まる。構築費は行数判定と古い順の削除、運用費は行数の確認のみ。', 'quantification': 'ヘッダ・明細を混在させて 20 万行とした場合、明細の混入比率によって総量が 60 MB から 400 MB の範囲で変動し、事前に一意へ定まらない。'} / free=Cloudflare D1 無料プランの 1データベース 500 MB 上限に対し、行数で抑えるため上限は定まるが、行あたりのサイズが層で 6 倍以上異なるため実際の総量は取込の内訳に依存する。 / fit=G4 に対して、遡れる期間が利用状況によって変わるという弱点を持つ。利用が軽い年は数年遡れる一方、大量取込が続いた月は数か月で古い記録が消え、利用者は『いつまで遡れるか』を事前に知ることができない。 / pros=容量の最大値が行数で定まり、上限到達の判定が単純である, 利用が軽い期間は結果的に長く遡れる / cons=『いつまで遡れるか』を利用者へ提示できず、監査記録としての予測可能性がない, 行あたりのサイズが層で大きく異なるため、行数上限が容量上限として機能しない, 大量取込の直後に古い記録が一気に消える / risks=重い明細が大量に入った月に、必要な古いヘッダまで巻き込んで消える, 利用者が遡れると思っていた期間と実際が食い違う / lock-in=低い。上限行数は定数であり運用で変えられる。 / ops=中。行数と実容量の乖離を継続して見る必要がある。 / evidence=https://developers.cloudflare.com/d1/platform/limits/ | opt-d8-two-tier — 監査記録に求められる期間は、記録の粒度によって性質が異なる。『いつ何を何件消したか』という操作の事実は、確定申告や前年同月比のように年をまたぐ振り返りで必要になるため、1年を越える保持が要る。一方『どの属性がどう変わり、どの規則で決まったか』という判定明細は、異常に気づいた直後に原因を辿るためのものであり、実務上は数か月あれば足りる。前者は1行あたり概ね 300 バイトと軽く、後者は概ね 2 キロバイトと重い。したがって、軽くて長く要るものと重くて短くてよいものを同じ保持規則に載せる合理性がない。opt-d8-align-d7 は規則が1本で済む代わりに、年単位の振り返りで監査記録が機能しなくなる。opt-d8-row-capped は行あたりのサイズが層で 6 倍以上異なるため、行数上限が容量上限として機能せず、遡れる期間を利用者へ提示できない。具体値は次のとおりとする。操作ヘッダ (audit_log) の保持は 400 日とし、確定申告の期をひとつ越えられるようにする。判定明細スナップショット (audit_log_detail) の保持は 90 日とする。ヘッダには操作種別・対象範囲・件数・実行時刻・結果のみを持たせ、明細内容と金額を含めない。掃除は D7 と同じく1回の呼び出しあたりのクエリ本数が 50 本未満に収まるよう分割実行し、層ごとに独立して走らせる。総量が単一データベース上限 500 MB の 60 パーセント (300 MB) に達した場合は、期間に達していなくても 判定明細側から古い順に掃除する。 (注意: 操作ヘッダへ明細内容や金額を載せないこと。載せると 90 日で消すはずの情報が 400 日残り、利用者の削除の意図と食い違う。, 層ごとの掃除を独立して走らせるため、片方が止まっても総量の監視が効いていないと気づけない。行数と使用量は層ごとに見ること。, 400 日の滞留は Time Travel の 7 日を大きく越えるため、監査記録テーブル自体の欠損は時点復旧では戻せない。, 400 日・90 日・300 MB という具体値は実データのない段階の見積りであり、実際の増え方を見て見直すことを前提とする。; confidence=中。ヘッダを年越しで残し明細を数か月で切るという層分けは、監査記録の用途の違いから導けており確度が高い。400 日・90 日という具体値と1行あたりのサイズ見積りは運用開始前の推定であり、調整を前提とする。; checked=2026-09-02T00:00:00Z) | opt-d8-two-tier @ 2026-09-02T00:00:00Z | G4, G5 |
