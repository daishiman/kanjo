---
status: confirmed
category: requirements-definition
---

# 要件定義書 (上位概念)

> 本章は spec-state.json の requirements_foundation を正本とする、システム構築の憲法。
> 以降の各技術章は frontmatter の serves_goals でここ (ゴール) へトレース (anchor) する。
> 上位概念がブレなければ、仕様が整った後もブレない。

- 確定マーカー: `status: confirmed`

## U1 本質的目的 (essential_purpose)

利用者が日々の記帳を止めずに完了できる状態を保つこと。そのために(1)本番データベースのスキーマをデプロイ済みコードが前提とする版へ一致させ続け、(2)利用者が使っていて気づいた不便や不具合を、その場の画面と状況ごと開発側へ届けて改善へつなげる経路を保つ。

## U2 背景 (background)

事故観測時、本番D1 (kanjo-db / fe9e732c-687c-4507-b704-1955b5367547) の d1_migrations は 0005_sub_vendors.sql までしか記録されておらず、migrations/0006〜0014 の9本が未適用だった。一方 Worker のコードは 0014 相当のスキーマを前提としており、POST /api/imports は acquireImportWriter が参照する import_writer_claims テーブル不在で、GET /api/imports は import_active_targets 参照で、いずれも D1 の no such table により 500 を返した。現在の repository head には 0015 も存在するため、0006〜0014 は事故時の事実に限定し、実際の適用対象は作業開始時の remote list から決める。原因はアプリのロジック不具合ではなく、デプロイ運用にスキーマ適用工程が組み込まれていないこと (人手依存で適用漏れが検出されないこと) である。

また、記帳を続けるなかで利用者が気づく不便や不具合は、これまで画面外の手段で伝えるほかなく、どの画面のどの状態で起きたのかが失われていた。調査の結果、改善要望を受け付ける機能はこのリポジトリに実装されたことがなく (削除コミット0件)、新規に設ける必要がある。改善作業は Claude Code / Codex で行うため、要望はそのまま着手できる指示文と証跡 (スクリーンショット・診断情報) の形で取り出せる必要がある。

## U3 ゴール (goals)

| ID | ゴール |
|---|---|
| G1 | 本番D1のスキーマをコードが前提とする最新版へ復旧し、データ取込と取込履歴が正常に動作する状態へ戻す |
| G2 | Migrate の人間承認による適用と Deploy の fail-closed 検査を分離し、コード配信とスキーマ適用が乖離したまま本番へ到達しない状態を構造的に保証する |
| G3 | 万一乖離した場合でも、利用者と開発者が原因を即座に特定できる検知と説明可能なエラー応答を備える |
| G4 | 復旧作業を通じて本番の既存データを一件も失わない |
| G5 | 利用者が気づいた改善点を、画面から離れず、そのとき見えていた画面と起きていた不具合ごと開発側へ届けられる状態にする |
| G6 | 受け取った改善要望を、Claude Code / Codex がそのまま着手できる指示文と証跡 (スクリーンショット・診断情報) として取り出せる状態にする |
| G7 | 送信・保存される診断情報を、原因特定に足りる最小範囲へ絞り、秘匿すべき値を含めない状態を構造的に保つ |

## U4 目標 (objectives)

| ID | 目標 | 測定基準 |
|---|---|---|
| O1 | 作業開始時の remote list と repository head から確定した承認済み pending manifest を本番D1へ適用完了する | manifest の全項目が適用済みで、同じ repository head / ordered migrations digest に対する未適用が0件 |
| O2 | 取込実行と取込履歴のエンドポイントが正常応答へ戻る | POST /api/imports と GET /api/imports が本番で 5xx を返さず、画面のサーバーエラー表示が消える |
| O3 | CI/CDのDeployがWorker配信前にmigration状態を読み取り検査する | 未適用が1件以上、または判定不能なら非ゼロ終了し、Worker配信へ進まない。Deployはmigrationを適用しない |
| O4 | スキーマ版数の不一致を実行時に検知する | 期待版と実際の適用版が不一致のとき、原因を示す専用エラーコードで応答し、汎用のサーバーエラー表示にしない |
| O5 | 適用前バックアップから復元可能な状態を確保する | 適用直前のバックアップが取得済みで、行数比較により適用前後のデータ件数が保全されている |
| O6 | 改善要望の投稿時に、モーダルが表示される前の画面を写したスクリーンショットが自動で添付される | 投稿された要望のスクリーンショットに改善要望モーダルの DOM が写り込んでいる件数が0件で、撮影失敗時も本文だけで投稿が成立する |
| O7 | 投稿済みの改善要望から、Claude Code / Codex 向けの指示文をコピーでき、指示文が指す API からエージェントがスクリーンショットと診断情報を取得できる | 指示文のコピー操作がコピー先 (claude_code / codex) 付きで記録され、指示文中の取得 URL がエージェント認証で 200 を返し画像と診断 JSON を取得できる |
| O8 | 添付される診断情報が、種別ごとの件数上限と総量上限で機械的に切り詰められる | 1件あたりの診断情報が定めた総バイト上限を超えず、切り詰めた場合は省略件数が記録される |
| O9 | 診断情報から秘匿値が除去される | Cookie / Authorization / Bearer トークン / パスワードに相当する値が保存前にマスクされ、テストで残存0件を確認できる |
| O10 | 改善要望に付いたスクリーンショットと診断情報が、対応完了から30日で自動削除される | 対応完了から30日を超えた要望の添付オブジェクトがストレージに残存0件で、要望本文と対応記録は参照可能なまま残る |
| O11 | 送信前に、一緒に送られる画面・表示環境・診断情報の中身を利用者自身が読める | 発生した画面・表示サイズ・取得時刻・ブラウザと、記録の1件ずつ (種類と要約) がモーダル内で読め、先に見るべき数件が全件の折りたたみより前に出る |
| O12 | 撮影されたスクリーンショットへ利用者が注記を書き込め、送信画像へ反映される | ドラッグで作った枠が送信される画像へ焼き込まれ、書き込みが0件のときは撮影した画像がそのまま送られる |

## U5 成功基準 (success_criteria)

- 承認済み pending manifest の全項目が本番の d1_migrations に記録され、同じ repository head / ordered migrations digest に対する未適用が0件である
- 本番画面でファイルを選び「取込を実行」してサーバーエラーが発生しない
- 取込履歴が「サーバー側で処理に失敗しました」を出さず一覧表示される
- 適用前後で mf_transactions / freee_deals / imports の行数が減少していない
- デプロイパイプラインで未適用がある、または状態を判定できない場合、Worker の新版が配信されない
- スキーマ版数不一致時のエラー応答が、汎用サーバーエラーではなく原因を特定できる内容である
- どの画面でも右下に固定された導線から改善要望を投稿でき、投稿後に一覧で内容・スクリーンショット・診断情報を確認できる
- 自動取得されたスクリーンショットに改善要望モーダルが写っておらず、そのとき見えていた本文が写っている
- 送信前に、一緒に送られる画面・表示環境・診断情報の中身を利用者自身が読める
- スクリーンショットへ枠を書き込め、その枠が送信される画像に残る
- 改善要望の詳細から Claude Code 向け / Codex 向けの指示文をコピーでき、AI分析と同じ操作感である
- 指示文を受け取ったエージェントが、追加の口頭説明なしにスクリーンショットと診断情報を取得して改善に着手できる
- エラーが起きていない要望では診断情報が空でも投稿が成立し、エラーがある要望では原因特定に足る診断情報が付く
- 保存された診断情報に秘匿値が含まれない
- 改善要望が対応完了になってから30日でスクリーンショットと診断情報が自動削除され、削除後も要望本文と対応記録は残る
- 改善要望のスクリーンショット・診断情報を保持するテーブルが夜間バックアップ (BACKUP_SNAPSHOT_SQL) の対象に含まれず、テストでその不在が固定されている

## U6 ステークホルダー (stakeholders)

- {'id': 'S1', 'role': '利用者 (個人事業の記帳担当)', 'need': '月次のCSV/ZIP取込を止めずに完了したい。エラー時は次に何をすべきか分かること'}
- {'id': 'S2', 'role': '開発兼運用者 (daishiman)', 'need': 'デプロイのたびにスキーマ適用を手動で思い出さなくてよいこと。乖離を即座に検知できること'}
- {'id': 'S3', 'role': '税務上の記録の正確性を担保する立場', 'need': '復旧作業で既存の会計データが失われないこと'}

## U7 スコープ (scope)

- **対象 (in)**: 作業開始時のremote listとrepository headから承認済みpending manifestを作る手順と検証, 適用前バックアップとデータ保全の確認手順, CI/CD Deployへの読み取り専用fail-closed検査の組み込み, 実行時のスキーマ版数不一致検知と、原因を特定できるエラー応答, 取込実行・取込履歴の本番動作確認, 改善要望の投稿・一覧・詳細と、投稿時のスクリーンショット自動取得 (モーダル表示前), 改善要望から Claude Code / Codex 向け指示文を生成・コピーする導線, 指示文が参照する、スクリーンショットと診断情報の取得用 API (使い捨てトークン認証), DevTools 相当の診断情報 (console error/warn・失敗した通信・未捕捉例外・実行環境) の上限付き収集と秘匿値マスク, 改善要望の添付 (スクリーンショット・診断情報) の保持期限管理と自動削除
- **対象外 (out)**: 取込パイプラインそのもののロジック変更 (パース/正規化/集計の仕様変更), マイグレーションファイル自体の追加・変更, Deployによるmigration自動適用, D1以外のストア (R2/KV) のスキーマ運用, 認証方式の変更, 改善要望を起点とした自動修正・自動 PR 作成 (指示文の提供までとし、改善作業は人間と AI が別途行う), 画面録画・操作リプレイの記録, 外部の課題管理サービスへの自動連携, 複数利用者間での改善要望の共有・権限分離 (単一利用者前提を維持する)

## U8 制約 (constraints)

- 実行基盤は Cloudflare Workers + D1 + R2 で、wrangler.jsonc の migrations_dir は ../../migrations である
- 本番D1への適用コマンドの実行主体は利用者本人とし、AIは手順の提示と検証までを担う (不可逆操作の承認を人間が握る)
- 適用対象は番号上限で固定せず、作業開始時と適用直前のremote listとrepositoryのmigrations/を突合して得たordered pending listを、人間がMigrateで承認したmanifestに限る
- manifestはrepository head・ordered migrations digest・remote applied head・ordered pending files・取得日時・人間の承認日時を持ち、承認後にrepository head・ordered migrations digest・remote listのいずれかが変われば承認を無効化して再取得する
- 恒久policyと通常のD1変更手順のSSOTはdocs/ci-cd-operations.mdとし、incident runbookはremote baseline・承認済みpending manifest・行数突合・証跡へのリンクだけを持つ
- 本番には既存データが存在し、データ喪失ゼロが必須である
- 0007/0010/0011 は cash_entries / attachments を再定義するため、適用順序の逸脱や部分適用を許さない
- D1 には1リクエストあたりのクエリ本数上限があり、取込処理は既存の query budget 設計 (上限未満50) を壊さない
- 明細内容や金額をログ・エラー応答へ含めない既存のセキュリティ方針を維持する
- スクリーンショット撮影はブラウザ内で完結させ、外部のレンダリングサービスへ画面内容を送出しない
- 診断情報は1件あたりの総バイト上限を設け、超過時は古いものから落として省略件数を記録する (無制限に保存しない)
- 診断情報の保存前に Cookie / Authorization / Bearer トークン / パスワード相当の値をマスクする
- 指示文が参照する取得用 API は既存 AI 分析と同じく使い捨て Bearer トークン (prefix 付き・ハッシュ保存・短命) とし、恒久トークンを発行しない
- スクリーンショットと診断情報は会計証憑ではなく税務保存義務の対象外として扱い、対応完了から30日で自動削除する
- 既存の夜間バックアップ (wrangler.jsonc の Cron "0 18 * * *" と scheduledMaintenance / nightlyBackup) は頻度・実装とも変更しない。改善要望の保持期限切れ削除は新規 Cron を増やさず既存 scheduledMaintenance へ1ジョブとして相乗りさせる
- 改善要望のスクリーンショット・診断情報を BACKUP_SNAPSHOT_SQL の列挙対象へ追加しない (追加すると対応完了から30日での削除がバックアップ側で最大30日ぶん骨抜きになるため)

## U9 具体的にやりたいこと (concrete_intents)

| ID | やりたいこと | 資するゴール |
|---|---|---|
| I1 | 本番D1の適用済みマイグレーションと migrations/ 配下の差分を機械的に列挙する | G1, G3 |
| I2 | 適用前バックアップの取得と、適用後の行数比較によるデータ保全確認を手順化する | G4 |
| I3 | remote listとrepository headから作った承認済みpending manifestを順序どおり本番へ適用する手順を、dry-runと結果確認込みで提示する | G1, G4 |
| I4 | デプロイワークフローに読み取り専用のmigration検査ステップを追加し、未適用または判定不能ならWorker配信へ進ませない | G2 |
| I5 | 期待スキーマ版と実適用版の不一致を実行時に検知し、専用エラーコードで返す | G3 |
| I6 | スキーマ不一致時の画面表示を、汎用サーバーエラーではなく次の行動が分かる文言にする | G3 |
| I7 | 適用後に取込実行と取込履歴が正常動作することを本番で確認する | G1 |
| I8 | 全画面から到達できる改善要望の投稿導線とモーダルを設け、本文と任意の補足を受け付ける | G5 |
| I9 | 改善要望ボタン押下時に、モーダルを DOM へ載せる前に画面を撮影し、撮影完了を待ってからモーダルを開く順序制御を行う | G5 |
| I10 | 撮影した画像を既存の縮小パイプラインで縮小してオブジェクトストレージへ保存し、要望レコードから参照する | G5, G7 |
| I11 | AI分析と同型の指示文生成を行い、Claude Code / Codex 向けのコピー導線とコピー先記録を提供する | G6 |
| I12 | 指示文が参照する取得用 API を、使い捨てトークン認証・短命・回数制限付きで提供し、スクリーンショットと診断情報を配信する | G6, G7 |
| I13 | console error/warn・失敗した通信・未捕捉例外・実行環境を、種別ごとの件数上限と総量上限、同一メッセージの集約、秘匿値マスクを課したうえで収集する | G7, G6 |
| I14 | 改善要望の添付 (スクリーンショット・診断情報) に対応済みからの保持期限を持たせ、期限超過分を定期的に削除する | G7 |

## 意思決定支援 (decisions)

| ID | 論点 | 状態 | 選択肢 (費用・適合・注意点) | AI推奨 | ユーザー決定 | 資するゴール |
|---|---|---|---|---|---|---|
| D1-migration-gate | コード配信とスキーマ適用の乖離を構造的に防ぐために、未適用マイグレーションの扱いをデプロイ工程へどう組み込むか。既存の migrate.yml による手動分離を保つ検査ゲート方式と、Deploy 内での自動適用方式のどちらを採るか。 | confirmed | opt-gate:Deploy ジョブの先頭に未適用マイグレーション検査ゲートを置き、未適用があれば配信前に停止する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '既存の GitHub Actions ジョブへ1ステップ追加するのみで追加費用は発生しない。実行時間の増分は数秒。'} / free=GitHub Actions の既存無料枠内で完結し、Cloudflare 側も wrangler d1 migrations list の読み取りのみで課金対象の書込を伴わない / fit=G2 の乖離防止を満たしつつ、G4 のデータ保全と不可逆操作の人手承認を維持する。今回のように9世代の乖離が本番へ到達する事態を配信前に必ず検出できる / pros=既存 migrate.yml の安全設計 (不可逆変更は人間が承認) を壊さない, 検査は読み取りのみで、ゲート自体がデータを変更しない, 停止時にエラーメッセージで次の行動 (Migrate を APPLY で実行) を示せる / cons=適用そのものは自動化されないため、開発者が Migrate を明示実行する手間が残る, Deploy が止まるため、適用を忘れているとリリースが一時的にブロックされる / risks=Cloudflare API 障害で検査ステップが失敗した場合に配信が止まる (fail-closed の意図した副作用として受容する) / lock-in=wrangler d1 migrations list の出力形式に依存するのみで、他基盤へ移す際の固定化は小さい / ops=低い。追加の常設インフラや監視対象を持たず、失敗時の対処手順が一本に定まる / evidence=https://developers.cloudflare.com/d1/reference/migrations/<br>opt-auto:Deploy ジョブ内で Worker 配信の前に migration を自動適用する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '追加費用なし。ただし事故時の復旧コスト (Time Travel からの復元と検証) を潜在的に抱える。'} / free=GitHub Actions の既存無料枠内で完結する / fit=乖離は原理的に発生しないが、既存の『DB変更は分離して手動』という設計方針を放棄する / pros=人手の介在を完全に排せる, 適用忘れという失敗モード自体が消える / cons=破壊的変更を含むマイグレーションでも無確認で本番へ走る, 既存 migrate.yml の確認文字列 APPLY による歯止めが実質無効化される / risks=誤ったマイグレーションが main にマージされた瞬間に本番データが失われ得る, 適用と配信が同一ジョブに同居するため、障害時に原因の切り分けが難しくなる / lock-in=低い / ops=平常時は最小だが、事故時の負荷が大きい / evidence=https://developers.cloudflare.com/d1/reference/migrations/<br>opt-hybrid:検査ゲートを置いたうえで、追加のみの安全なマイグレーションだけ自動適用する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '追加費用はないが、破壊的変更を判定するロジックの実装と継続的な保守コストが発生する。'} / free=GitHub Actions の既存無料枠内で完結する / fit=G2 を満たすが、判定ロジックの誤りが G4 のデータ保全を直接損なう / pros=日常的な追加系マイグレーションでは人手が不要になる, 破壊的変更に対しては承認を残せる / cons=SQL の静的解析で破壊性を判定する必要があり実装が重い, テーブル再作成を伴う移行 (本件の 0007/0010/0011 が該当) の分類が曖昧になる / risks=安全と誤判定された破壊的変更が無確認で適用される, 判定ロジック自体がテスト対象として増え、保守されないまま形骸化する / lock-in=判定ロジックが自作資産となり、移植時の負債になる / ops=中程度。判定ロジックの信頼性検証が恒常的に必要 / evidence=https://developers.cloudflare.com/d1/reference/migrations/ | opt-gate — 既存 migrate.yml の分離は『本番DBの不可逆変更は人間が承認する』という妥当な安全設計であり、今回の障害はその設計が悪かったからではなく、乖離を検出する門が無かったことに起因する。検査ゲートは Deploy の入口に読み取り1ステップを足すだけで、未適用があれば配信前に fail-closed で停止するため、安全設計を保ったまま欠けていた検出を補える。公式ドキュメントのとおり適用済み状態は d1_migrations テーブルが正本であり、wrangler の list コマンドで未適用分を機械的に判定できる。 (注意: 検査は wrangler d1 migrations list の未適用件数を判定源とし、d1_migrations テーブルを直接書き換える運用を作らない, ゲートが停止した際に開発者が何をすべきか (Migrate ワークフローを APPLY で実行する) をエラーメッセージへ明示する, 検査ステップ自体が Cloudflare API 障害で失敗した場合も fail-closed とし、検査不能を成功と扱わない; confidence=high; checked=2026-08-27T02:45:59Z) | opt-gate @ 2026-08-27T02:45:59Z | G2, G4 |
| D2-pre-apply-backup | 本番D1へ承認済みpending manifestを適用する直前に、データ喪失ゼロ (G4) を担保するバックアップをどの方式で取得するか。 | confirmed | opt-export:適用直前に wrangler d1 export --remote で全件を SQL ダンプとしてローカルへ取得する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '追加費用なし。エクスポートは D1 の読み取り操作で、保管先は開発者のローカルのため保管コストも発生しない。所要は数十秒。'} / free=D1 の読み取り行数として計上されるのみで、現行の利用規模では無料枠内に収まる / fit=G4 のデータ喪失ゼロを、現物のダンプという最も直接的な形で担保する。ダンプ内の行数をそのまま適用後の行数比較の基準に使えるため O5 の測定手段も同時に満たす / pros=取得物を目視でき、復元可能であることを事前に確認できる, 適用前後の行数比較の基準値をダンプから直接得られる, Cloudflare 側の状態に依存せず手元に現物が残る / cons=明細を含むファイルをローカルに置くため、作業後の削除まで含めた取り扱いが要る, データ量が増えると取得時間が伸びる / risks=エクスポート中にデータが更新されると、ダンプと適用直前の状態がわずかにずれる (作業時間帯を利用者が使わない時間に限ることで回避する) / lock-in=出力は標準的な SQL のため、他の SQLite 系基盤へも持ち出せる。固定化はほぼない / ops=低い。1コマンドで完結し、常設インフラを増やさない / evidence=https://developers.cloudflare.com/d1/best-practices/import-export-data/<br>opt-time-travel:D1 Time Travel のブックマークだけを控え、事故時はそこへ復元する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '追加費用なし。Time Travel は D1 に標準で備わる。'} / free=保持期間の範囲内であれば追加課金なく利用できる / fit=復元は可能だが、適用前の中身を事前に確認できないため G4 の担保が『Cloudflare 側が正しく保持していること』への信頼に依存する / pros=追加操作がほぼ不要で、手順が最短になる, 明細を含むファイルを手元に作らない / cons=適用前の中身を事前に検証できない, 行数比較の基準値を別途取得する必要がある / risks=保持期間を過ぎた後に問題が発覚すると復元できない, 復元操作自体が本番DBの上書きであり、それ自体が不可逆 / lock-in=Cloudflare D1 固有機能のため、他基盤では同等の手段を作り直す必要がある / ops=低い / evidence=https://developers.cloudflare.com/d1/reference/time-travel/<br>opt-both:export と Time Travel ブックマークの両方を取得する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '追加費用なし。手順が1ステップ増えるのみ。'} / free=両方とも無料枠内で完結する / fit=G4 を二重に担保する。手元の現物と基盤側の復元点の両方を持つ / pros=片方が使えない事態でももう一方で復旧できる, 検証手段と復元手段を分離できる / cons=手順が増え、作業の所要時間が伸びる, 今回の規模では冗長になりやすい / risks=手順が増えることで、作業者が一部を飛ばす余地が生まれる / lock-in=opt-export と同等 / ops=低い / evidence=https://developers.cloudflare.com/d1/best-practices/import-export-data/ | opt-export — 本件で最も重要なのは『適用前に何があったかを後から確認できること』である。取込データの正確性は税務上の記録に直結し、行数が減っていないという事後検証 (O5 の測定基準) には基準値の現物が要る。Time Travel は復元手段としては有効だが、適用前の中身を事前に確認できず、行数比較の基準を別途取らねばならない。加えて既存の夜間 cron バックアップは cash_entries / attachments / restored_monthly_agg を参照する SQL を持ち、これらのテーブルが本番に存在しないため毎晩失敗し続けており、復旧前のバックアップ手段として当てにできない。したがって適用直前の明示的な export を基本線に据える。 (注意: 取得したダンプは明細と金額を含むため、作業完了後に削除するところまでを手順に含める, エクスポート中の書き込みを避けるため、利用者がアプリを操作しない時間帯に実施する, 既存の夜間バックアップは本番で失敗し続けているため、復旧完了後に正常化を確認する; confidence=high; checked=2026-08-27T03:38:13Z) | opt-export @ 2026-08-27T04:28:22Z | G4, G1 |
| D3-runtime-schema-guard | デプロイ済みコードが前提とするスキーマ版と、本番D1に実際に適用済みの版が食い違ったとき、実行時のどこでそれを検知するか。 | confirmed | opt-entry-middleware:session/token 認証後の共通 middleware で期待版と d1_migrations の最新版を照合し、不一致なら専用エラーコードで返す / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '追加費用なし。照合結果を isolate 内に期限付きでキャッシュすれば D1 への問い合わせはキャッシュ有効期間あたり1回に収まる。'} / free=D1 の読み取り1行の問い合わせがキャッシュ有効期間ごとに1回増えるのみで、現行規模では無料枠に影響しない / fit=G3 の『乖離を即座に特定できる』を認証済みの全 D1 利用エンドポイントで満たす。今回のように取込と取込履歴の両方が同時に落ちる事態でも、どちらも同じ原因を名指しで返せる / pros=乖離が起きた瞬間から認証済みの全 D1 利用経路で確実に検知できる, 利用者向けの説明可能なエラー応答と、開発者向けのログを同じ判定点から出せる, 検知漏れの経路が原理的に存在しない / cons=認証済みの全 D1 利用リクエストが判定を通るため、キャッシュ設計を誤ると D1 への問い合わせが増える, middleware の順序を誤ると認証前に DB へ触れる経路が生まれる / risks=照合自体が失敗した場合も fail-closed 503 とするため、D1 の一時障害中は認証済みの業務 API を停止する。判定不能な schema へ業務クエリを流さない安全性を優先する / lock-in=d1_migrations テーブルの読み取りに依存するのみで、固定化は小さい / ops=低い。判定点が一箇所に集まるため、挙動の確認とテストが容易 / evidence=https://developers.cloudflare.com/d1/reference/migrations/<br>opt-heavy-endpoints:取込など、スキーマ依存が強い重い処理の直前でだけ照合する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '追加費用なし。判定回数が最小になる。'} / free=問い合わせ回数が最も少なく、無料枠への影響は無視できる / fit=取込は救えるが、取込履歴や他の一覧系で起きた乖離は素通りして汎用サーバーエラーのままになる。今回の障害は両方で起きているため G3 を部分的にしか満たさない / pros=オーバーヘッドが最小, 影響範囲が限定的で導入しやすい / cons=判定を入れ忘れた経路の乖離は検知できない, 新しいエンドポイントを追加するたびに判定の要否を判断し続ける必要がある / risks=判定箇所の網羅が人手の注意力に依存し、時間とともに形骸化する / lock-in=小さい / ops=中程度。判定箇所の網羅を維持し続ける手間が残る / evidence=https://developers.cloudflare.com/d1/reference/migrations/<br>opt-cron-monitor:既存の夜間 cron ジョブで照合し、不一致をログへ残す / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '追加費用なし。既存 cron へ1処理追加するのみ。'} / free=1日1回の問い合わせのため無料枠への影響はない / fit=検知が最大24時間遅れ、利用者の画面表示は汎用サーバーエラーのまま変わらない。G3 の利用者側の要求を満たさない / pros=リクエスト経路への影響がゼロ, 実装が最も小さい / cons=利用者は依然として原因不明のサーバーエラーに遭遇する, 検知が最大1日遅れる / risks=既存の夜間ジョブ自体が現在 cash_entries 不在で失敗し続けており、そこへ相乗りすると検知処理も同時に失われる / lock-in=小さい / ops=低いが、ログを見に行く運用が前提になる / evidence=https://developers.cloudflare.com/d1/reference/migrations/ | opt-entry-middleware — 今回の障害は取込 (POST /api/imports) と取込履歴 (GET /api/imports) の両方で同時に起きており、特定のエンドポイントに閉じた検知では取りこぼす実例がすでに出ている。session/token 認証後の共通入口で照合すれば、認証前 DB access を増やさず業務 D1 経路の検知漏れを防げる。d1_migrations は公式ドキュメントのとおり適用済み状態の正本であり、その最新レコードとビルド時に確定する期待版を突き合わせるだけで判定できる。照合結果を isolate 内に期限付きでキャッシュすれば、毎リクエストの問い合わせも避けられる。 (注意: 照合結果は isolate 内に期限付きでキャッシュし、毎リクエストで D1 へ問い合わせない, 照合そのものが失敗した場合も専用 503 とし、判定不能な schema へ業務クエリを進めない。public auth は対象外とし、guard は認証後に置く, 応答にはテーブル名やスキーマ構造を含めず、専用コードと利用者向け文言だけを返す; confidence=high; checked=2026-08-27T03:38:13Z) | opt-entry-middleware @ 2026-08-27T04:28:22Z | G3 |
| D4-schema-mismatch-user-message | スキーマ版数の不一致を検知したとき、利用者の画面へどこまでの情報を、どういう表現で示すか。 | confirmed | opt-actionable-wait:復旧作業中である旨と、利用者が取るべき行動 (時間をおいて再試行) を示す文言を表示する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '追加費用なし。フロントのエラー分岐に1件追加するのみ。'} / free=該当なし (クライアント側の表示分岐のみ) / fit=G3 の『利用者が原因を即座に特定できる』を、利用者にとって意味のある粒度で満たす。原因の技術的中身ではなく『自分の操作の誤りではない』『待てば直る』という判断材料を渡す / pros=利用者が同じ操作を無駄に繰り返したり、自分のファイルを疑ったりしなくて済む, 内部構造を露出しない, 既存の汎用サーバーエラー表示と明確に区別できる / cons=開発者が画面だけを見て原因を特定することはできず、ログか専用コードを見る必要がある / risks=『しばらく待てば直る』と示す以上、実際に復旧されない状態が続くと文言が嘘になる (検知と復旧手順が対で運用されることが前提) / lock-in=なし / ops=低い / evidence=https://developers.cloudflare.com/d1/reference/migrations/<br>opt-technical-detail:未適用マイグレーションが存在する旨と参照コードを画面へ併記する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '追加費用なし。'} / free=該当なし / fit=開発者が画面だけで原因を特定できる。ただし本アプリの利用者は記帳担当であり、技術的詳細は行動の判断材料にならない / pros=開発者が原因を最短で特定できる, 問い合わせのやり取りが減る / cons=利用者にとって意味のない情報が前面に出る, 内部構造の露出が増える / risks=画面のスクリーンショットを外部へ共有した際に内部運用状態が漏れる / lock-in=なし / ops=低い / evidence=https://developers.cloudflare.com/d1/reference/migrations/<br>opt-log-only:画面表示は汎用サーバーエラーのまま変えず、ログとエラーコードだけを区別する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '追加費用なし。実装が最小。'} / free=該当なし / fit=開発者側の特定は速くなるが、利用者は今回と同じ『サーバーエラーが発生しました』に留まる。G3 の利用者側の要求を満たさない / pros=実装が最小, 既存のエラー表示を一切変えない / cons=利用者の体験は今回の障害時と変わらない, 本件の出発点である『次に何をすべきか分からない』が解消しない / risks=対策したつもりで利用者側の課題が残り続ける / lock-in=なし / ops=低い / evidence=https://developers.cloudflare.com/d1/reference/migrations/ | opt-actionable-wait — 本件の出発点は、利用者が『取込を実行できませんでした: サーバーエラーが発生しました』を見て次に何をすべきか分からない状態に置かれたことである。利用者 (S1) にとって必要なのは未適用マイグレーションという内部事情ではなく、自分のファイルや操作が悪いのではないこと、そして待てば解決することの2点である。専用エラーコードで開発者向けの識別は確保しつつ、画面には行動可能な文言だけを出すことで、既存のセキュリティ方針を崩さずに G3 を満たせる。 (注意: 画面文言にテーブル名・スキーマ版数・スタックトレースを含めない, 専用エラーコードは応答とログの双方に載せ、開発者はそちらで特定する, 『しばらく待てば直る』と示す以上、検知時に開発者へ届く経路 (ログ) を必ず併設する; confidence=high; checked=2026-08-27T03:38:13Z) | opt-actionable-wait @ 2026-08-27T04:28:22Z | G3 |
| D5-pre-modal-screenshot-capture | 改善要望ボタン押下時に「モーダルが表示される前の画面」をどの方式で撮影するか | confirmed | opt-dom-capture-before-open:DOM を canvas へ描画するライブラリ (html2canvas) をボタン押下時に動的 import し、撮影完了を待ってからモーダルを open する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': 'MIT ライセンスの OSS で利用料0円。動的 import により初期バンドルへ加算されず、改善要望ボタンを押した利用者だけが約40KB(gzip)を追加取得する。'} / free=OSS のため制限なし。配信は既存の Cloudflare Pages 無料枠に含まれる。 / fit=撮影がモーダル open より前に完了することが制御フロー上保証されるため、G5 の「そのとき見えていた画面ごと届ける」を型で担保できる。追加の利用者操作が0回である点も G5 に適合する。 / pros=利用者の追加操作・追加許可が一切不要で、ボタン1回で完結する, 撮影範囲がアプリの DOM に限定され、無関係な情報の混入が構造的に起きない, 撮影 Promise を await してから setOpen(true) するだけで順序が保証され、モーダル除外指定のような後追いの回避策が不要になる / cons=外部 iframe や一部の CSS (backdrop-filter 等) の再現が完全ではない, 描画コストがあり、要素数の多い画面では数百ミリ秒の待ちが発生する, OSS への依存が1つ増える / risks=描画失敗時にモーダルが開かないと機能自体が使えなくなるため、撮影失敗でも必ずモーダルを開き本文だけで投稿できる縮退経路が必須になる / lock-in=低。撮影部分は単一関数に閉じるため、別ライブラリや将来の標準 API へ差し替えられる。 / ops=低。依存の更新追従のみ。 / evidence=https://github.com/niklasvh/html2canvas, https://developer.mozilla.org/en-US/docs/Web/API/HTMLCanvasElement/toBlob<br>opt-render-modal-then-exclude:先にモーダルを開き、撮影時にモーダル要素を除外指定 (ignoreElements) して撮る / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '同じライブラリを使うため費用は同等。'} / free=OSS のため制限なし。 / fit=利用者から見た結果は近いが、除外指定の網羅性に依存するため G5 の「モーダル表示前の画面」を保証できない。 / pros=モーダルが即座に開くため体感の待ち時間がない, 撮影を非同期で後追いでき、UI の応答性が保たれる / cons=モーダル・オーバーレイ・ポータル・トーストなど除外対象を列挙し続ける必要があり、UI 追加のたびに漏れる, モーダル表示による背景のスクロールロックや blur が背景の見え方を変えてしまう, 利用者が明示した要求 (モーダル表示前の画面) を実装の都合で近似することになる / risks=除外漏れが起きても画像を見るまで気づけず、静かに要求違反の成果物が蓄積する / lock-in=低。 / ops=中。UI 要素追加のたびに除外リストの保守が必要。 / evidence=https://github.com/niklasvh/html2canvas<br>opt-screen-capture-api:ブラウザ標準の Screen Capture API (getDisplayMedia) で画面を取得する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '標準 API のため依存追加も費用も0。'} / free=ブラウザ標準機能のため制限なし。 / fit=低い。取得のたびに利用者へ共有対象の選択ダイアログが出るため、「気づいたときに一手で送る」という G5 の体験を大きく損なう。 / pros=実際に表示されているピクセルをそのまま取得でき再現性が最も高い, 外部依存を追加しない / cons=利用者へ許可ダイアログと共有対象の選択を毎回強いる, 選択ダイアログ自体が「モーダル表示前の画面」を崩す, モバイルブラウザでの対応が限定的 / risks=利用者が誤って画面全体を共有し、アプリ外の秘匿情報がスクリーンショットとして保存される / lock-in=低。 / ops=低。ただし利用者からの問い合わせ対応は増える。 / evidence=https://developer.mozilla.org/en-US/docs/Web/API/Screen_Capture_API/Using_Screen_Capture | opt-dom-capture-before-open — 利用者の要求は「モーダルが表示される前の画面を撮る」であり、撮影完了を待ってからモーダルを open するという制御フローはこの要求を近似ではなく順序で保証する。除外指定案は UI が増えるたびに漏れが生じ、しかも漏れても画像を見るまで気づけない。Screen Capture API は許可ダイアログが体験を壊すうえ、アプリ外の情報が混入しうる点で G7 に反する。動的 import により初期バンドルへ影響しない点も既存の軽量な web パッケージ構成と整合する。 (注意: 撮影失敗でも必ずモーダルを開き、スクショなしで投稿できる縮退経路を実装すること, 要素数の多い画面では数百ミリ秒の遅延が出るため、押下直後にローディング表示を出すこと, 外部 iframe や一部 CSS の再現が完全でないため、画像は「参考」であり本文の記述を代替しないこと; confidence=high; checked=2026-08-30T00:00:00Z) | opt-dom-capture-before-open @ 2026-08-30T09:20:00Z | G5, G6 |
| D6-improvement-artifact-retention | 改善要望に付随するスクリーンショットと診断情報を、いつ削除するか (保持期間と削除トリガ) | confirmed | opt-retain-30d-after-done:対応完了(done)から30日後に添付スクショと診断情報だけを削除し、要望本文と状態は残す / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': 'R2 標準ストレージ無料枠10GB/月の範囲内。月50件×(スクショ約200KB+診断約30KB)≒12MBで、30日保持でも常時12MB程度しか滞留しない。Cron Trigger も Workers 無料枠に含まれ追加費用0。'} / free=R2 Standard storage 10 GB-month, Class A 1,000,000 req/月, Class B 10,000,000 req/月 (無料枠)。Workers Cron Triggers は Workers プランに含まれる。 / fit=G5(要望を届ける)には十分な期間で、G7(秘匿すべき値を最小範囲に留める)に対して滞留期間を最短化できる。改善対応の実作業は通常数日〜2週間で完了するため30日で再確認の余地も残る。 / pros=秘匿情報の滞留期間が最短になり漏えい時の影響範囲が小さい, 容量が常時十数MBに収まり無料枠を一切圧迫しない, 要望本文と対応状態は残るので改善履歴の振り返りが可能 / cons=30日より後に「あのときの画面をもう一度見たい」となっても復元できない, 削除ジョブの失敗を検知する仕組みが別途必要 / risks=Cron Trigger が失敗し続けると削除されないまま滞留する。件数記録と失敗カウントの可観測性が必要 / lock-in=低。削除ポリシーはアプリ側のジョブで実装するため、R2/D1 以外へ移行しても同じ方針を移植できる。 / ops=低。日次 Cron 1本と削除件数のログのみ。 / evidence=https://developers.cloudflare.com/r2/pricing/, https://developers.cloudflare.com/workers/configuration/cron-triggers/<br>opt-retain-90d-after-done:対応完了から90日後に削除する (証憑添付と同じ長期保持に寄せる) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '同条件で常時約36MB滞留。これも無料枠内だが滞留量は3倍。'} / free=R2 Standard storage 10 GB-month の無料枠内。 / fit=長期の振り返りには有利だが、G7 の最小保持という目的からは後退する。 / pros=四半期単位の振り返りで過去の要望画面を参照できる, 証憑添付の保持ポリシーと期間を揃えられ運用の説明が単純になる / cons=秘匿情報の滞留期間が3倍になる, 改善作業は通常30日以内に完了するため大半の期間は死蔵データになる / risks=本来消えているはずの画面情報が長期間残り、目的外利用や誤共有の余地が生まれる / lock-in=低。 / ops=低。ただし滞留量の監視対象がやや増える。 / evidence=https://developers.cloudflare.com/r2/pricing/, https://developers.cloudflare.com/r2/buckets/object-lifecycles/<br>opt-manual-delete-only:自動削除を設けず、利用者が明示的に削除したときだけ消す / cost={'category': 'unknown', 'amount': None, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '削除されない前提では単調増加。年600件で約140MB/年、数年で無料枠内には収まるが上限が定義できない。'} / free=R2 Standard storage 10 GB-month。ただし増加が止まらないため無料枠内である保証を設計時点で与えられない。 / fit=G5 には支障ないが、G7 の「秘匿すべき値を含めない状態を構造的に保つ」を人手の運用に依存させてしまう。 / pros=実装が最も単純で削除ジョブが不要, 利用者が必要と判断した限り情報を失わない / cons=実質無期限保持となり最小保持の原則に反する, 容量の上限を設計時点で保証できない / risks=秘匿情報が無期限に蓄積し、将来の漏えい時の影響範囲が時間とともに拡大し続ける / lock-in=低。 / ops=見かけ上0だが、実際には人手の削除運用が恒常的に必要になる。 / evidence=https://developers.cloudflare.com/r2/pricing/ | opt-retain-30d-after-done — 改善要望の実作業は数日〜2週間で完了するため30日で運用上の不足はなく、最も秘匿性の高いスクショと診断情報の滞留期間を最短化できる。容量は常時十数MBでR2無料枠10GBに対して桁違いに小さく、保持期間を延ばす費用上の動機が存在しない。要望本文と対応状態は削除対象外とすることで改善履歴の追跡性は維持される。 (注意: 30日経過後は当時の画面を復元できないため、恒久的に残したい知見は要望本文か別ドキュメントへ転記する運用が要る, 削除ジョブが失敗し続けると滞留するため、対象件数・削除件数・失敗件数の記録が前提条件になる, R2 オブジェクトだけが残る孤児化に備え、定期的な突合が必要; confidence=high; checked=2026-08-30T00:00:00Z) | opt-retain-30d-after-done @ 2026-08-30T00:00:00Z | G5, G7 |
| D7-improvement-backup-cadence | 既存の夜間バックアップ (packages/api/src/index.ts の nightlyBackup。JST 03:00 の Cron で canonical テーブルを統合 JSON 化し R2 backups/YYYY-MM-DD.json へ保存、30日より古いものを削除) の実行頻度を維持するか、下げるか、廃止するか。あわせて改善要望データをこのバックアップ対象に含めるか | confirmed | opt-keep-daily-exclude-improvement-artifacts:日次のまま維持し、改善要望のスクリーンショットと診断情報はバックアップ対象へ含めない / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '統合 JSON は20ヶ月ぶんの明細でも数MB規模。30世代でも数十〜百数十MBで、R2 Standard storage 無料枠10GB/月に対して1〜2%程度。Class A オペレーションは1日あたり put 1回 + list/delete 数十回で月1000回前後、無料枠1,000,000回に対して0.1%未満。追加費用は実質0。'} / free=R2 Standard storage 10 GB-month / Class A 1,000,000 req 月 / Class B 10,000,000 req 月。Workers Cron Triggers は Workers プランに含まれる。 / fit=U1 の中核である記帳データ (事業・家計の明細と残高) の復旧目標時点を最大24時間に保てる。改善要望の添付物は対象外とすることで D6 の30日削除が確実に効き、G7 の最小保持と両立する。 / pros=利用者の一次資産である記帳データの復旧目標時点が最大24時間に保たれる, 実費が事実上0で、頻度を下げても得られる節約がない, 既存実装・既存テストへの変更が0件で、退行リスクを持ち込まない, 改善要望テーブルを列挙しなければ D6 の30日削除が既定で貫徹される / cons=統合 JSON が毎日1件ずつ増えるため、R2 のオブジェクト一覧は30件常駐する, 記帳データの日次スナップショットが30世代分、R2 に滞留し続ける / risks=将来 BACKUP_SNAPSHOT_SQL へ改善要望テーブルを不用意に追加すると、D6 の削除が形骸化する。仕様上の禁止事項として明記し、テストで固定する必要がある / lock-in=低。統合 JSON という可搬形式のため、D1 以外へ移行しても復元経路を維持できる。 / ops=変更なし。既存の scheduledMaintenance のログと失敗検知をそのまま使う。 / evidence=https://developers.cloudflare.com/r2/pricing/, https://developers.cloudflare.com/workers/configuration/cron-triggers/<br>opt-weekly-full-export:バックアップの書き出しを週次へ落とす (Cron は日次のまま、書き出し判定を週1回にする) / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '世代数が30から4-5へ減り、ストレージと Class A オペレーションが約1/7。ただし元の絶対量が無料枠の1〜2%であるため、削減額は0円のまま変わらない。'} / free=同上。 / fit=低下する。記帳データの復旧目標時点が最大24時間から最大7日へ後退する。U1 の一次資産に対する保全水準を、費用上の見返りがないまま下げることになる。 / pros=R2 に常駐するバックアップ世代が30件から4-5件へ減る, 書き出し処理の実行回数が減る / cons=記帳データの復旧目標時点が最大7日へ後退する, 削減されるのは無料枠の1〜2%にすぎず、費用面の見返りが0円, 既存の nightlyBackup へ曜日判定を足す変更が必要で、退行リスクとテスト追加が発生する / risks=曜日判定の実装ミスで書き出しが長期間行われず、気づかないまま保全が失われる / lock-in=低。 / ops=微増。書き出しが行われない日が正常なのか異常なのかをログから判別できるようにする必要がある。 / evidence=https://developers.cloudflare.com/r2/pricing/, https://developers.cloudflare.com/workers/configuration/cron-triggers/<br>opt-drop-backup-rely-on-time-travel:統合 JSON バックアップを廃止し、D1 Time Travel の復元だけに委ねる / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': 'Time Travel は D1 標準機能で追加費用なし。R2 のバックアップ容量とオペレーションが0になるが、元々0円のため削減額も0円。ただし復元可能期間はプランに依存し、無料プランでは7日に制限される。'} / free=D1 Time Travel の復元可能範囲は Workers Paid プランで過去30日、Workers Free プランでは過去7日である (https://developers.cloudflare.com/d1/reference/time-travel/ 2026-08-30 確認)。本プロジェクトは無料枠運用を前提としているため、この案を採ると実効的な復元範囲は7日となり、現行の R2 バックアップ30世代 (=30日) を下回る。 / fit=D1 が健在である限り Time Travel の粒度は日次スナップショットより細かく優れる。しかし復元可能期間が無料プランでは7日に留まり、現行の30世代 (30日) を下回る。加えて D1 データベース自体の消失やアカウント側の事故に対する独立コピーが失われ、U1 の一次資産の保全としては一点依存になる。 / pros=復元可能期間の内側であれば任意時点へ復元できるため、粒度は日次スナップショットより細かい, R2 上のバックアップ滞留が完全になくなる, 維持すべきジョブが1つ減る / cons=無料プランでは復元可能期間が7日に制限され、現行の30日ぶんの世代を下回る, D1 の外に独立したコピーが1つも残らない, Time Travel は D1 固有機能のため、可搬な形式での退避手段を失う, 既存の実装とテストを削除する変更が必要 / risks=D1 データベースの削除やアカウント側の事故が起きた場合、記帳データを復旧する手段が一切残らない, 無料プラン運用のまま7日を超えて誤りに気づいた場合、復元手段が存在しない / lock-in=高。復元手段が D1 固有機能のみになる。 / ops=最小。ただし復旧手段が単一化することの説明責任が残る。 / evidence=https://developers.cloudflare.com/d1/reference/time-travel/, https://developers.cloudflare.com/d1/platform/limits/ | opt-keep-daily-exclude-improvement-artifacts — 頻度を下げる動機は通常コストだが、本件では実費が3案とも0円で、日次を週次にしても削減額は0円のまま復旧目標時点だけが24時間から7日へ後退する。バックアップ対象は利用者の一次資産である記帳データであり、費用上の見返りなしに保全水準を下げる根拠がない。廃止案は Time Travel の粒度こそ優れるが、その30日という復元範囲は Workers Paid プラン限定であり、無料枠運用の本プロジェクトでは7日に縮む。現行の30世代を下回るうえ、D1 の外に独立コピーが1つも残らなくなる点で一点依存を招く。したがって既存の日次バックアップはそのまま維持する。一方、利用者が懸念した「毎日ぶん溜まり続けること」への対処は頻度ではなく対象の限定で行う。改善要望のスクリーンショットと診断情報をバックアップ対象へ含めないことで、D6 で確定した30日削除が複製側に阻まれず貫徹される。BACKUP_SNAPSHOT_SQL が canonical テーブルを明示列挙する設計のため、新テーブルを列挙しないことがそのまま除外になる。 (注意: BACKUP_SNAPSHOT_SQL へ改善要望テーブルを追加しないことを仕様上の禁止事項として明記し、テストで固定すること, 統合 JSON は R2 原本 (添付ファイル実体) を含まないため、バックアップだけで完全復元できるとは主張しないこと。既存実装がこの点を inventory/archive 契約として明示している方針を維持する, 既存の nightlyBackup の実装・Cron 設定は変更しない。今回のスコープは対象テーブルの限定だけに留める; confidence=high; checked=2026-08-30T00:00:00Z) | opt-keep-daily-exclude-improvement-artifacts @ 2026-08-30T00:00:00Z | G1, G5, G7 |
| D8-diagnostics-collection-timing | DevTools 相当の診断情報 (未捕捉例外・失敗リクエスト・console error/warn) をいつ収集するか | confirmed | opt-always-on-ring-buffer:アプリ起動時から常時、上限つきリングバッファへ記録し、投稿時にその内容を添付する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': 'メモリ上の固定長バッファのみ。送信は投稿時の1回で、常時通信は発生しない。追加インフラ0。'} / free=クライアント内メモリのみを使うため外部サービスの制限を受けない。 / fit=エラーは改善要望ボタンを押す「前」に起きる。常時記録だけが、利用者が異変に気づいた時点より前の事象を捕捉でき、G5・G6 の「起きていた不具合ごと届ける」を成立させる。 / pros=ボタン押下より前に発生したエラーを確実に含められる, リングバッファなので古い記録は自動的に落ち、メモリ使用量に上限がある, 投稿しなければ一切外部へ出ないため、常時記録と最小送信を両立できる / cons=アプリ起動時にグローバルハンドラを差し込む必要があり、初期化順序の管理が要る, console をラップするため、開発時のスタックトレース表示に影響しないよう注意が必要 / risks=マスク処理に漏れがあると秘匿値がバッファに滞留する。収集時とサーバ受信時の二重マスクが必要 / lock-in=低。自前実装のため外部サービスへの依存がない。 / ops=低。 / evidence=https://developer.mozilla.org/en-US/docs/Web/API/Window/error_event, https://developer.mozilla.org/en-US/docs/Web/API/Window/unhandledrejection_event<br>opt-collect-on-modal-open:改善要望モーダルを開いた時点で、その時に取得できる情報だけを収集する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '常時処理がなく最も軽い。追加インフラ0。'} / free=外部サービスを使わないため制限なし。 / fit=低い。過去のエラーは既に失われており、利用者が報告したい事象そのものを取り逃す可能性が高い。 / pros=常時のフックが不要で実装が単純, 収集する情報量が最小 / cons=報告対象のエラーは押下より前に起きているため、肝心の情報がほぼ取れない, 取得できるのは環境情報と現在ルート程度に限られる / risks=「診断情報を添付している」という体裁だけが整い、実際には原因特定に使えないデータが送られ続ける / lock-in=低。 / ops=低。 / evidence=https://developer.mozilla.org/en-US/docs/Web/API/Window/error_event<br>opt-external-error-tracking-service:外部のエラートラッキング SaaS を導入し、そこへ送られた情報を改善要望から参照する / cost={'category': 'unknown', 'amount': None, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '無料枠のあるサービスも存在するが、イベント数に応じて課金される従量制が一般的で、上限を設計時点で固定できない。'} / free=サービスにより異なり、月あたりのイベント数上限が設定されるのが一般的。 / fit=エラー捕捉能力は最も高いが、改善要望1件と紐づけて Claude Code へ渡すという G6 の要件に対しては、外部サービスの参照を挟むぶん経路が長くなる。 / pros=ソースマップ対応やリリース単位の集計など高度な機能を最初から使える, エラーの発生傾向を横断的に把握できる / cons=エラー内容が常時、第三者サービスへ送出される, 改善要望1件と紐づけるための追加の連携実装が必要, 無料枠を超えた場合の費用が予測しにくい / risks=記帳内容に由来する情報が意図せず外部サービスへ送られる, サービスの料金体系変更や終了に事業継続が左右される / lock-in=高。SDK・ダッシュボード・データの所在がサービス依存になる。 / ops=中。アカウント・プロジェクト・DSN・保持設定の継続管理が必要。 / evidence=https://developer.mozilla.org/en-US/docs/Web/API/Window/unhandledrejection_event | opt-always-on-ring-buffer — 利用者が改善要望ボタンを押すのは異変に気づいた「後」であり、原因となったエラーはその前に発生している。モーダル open 時収集では肝心の情報が既に失われているため、目的を果たせない。外部 SaaS は捕捉能力こそ高いが、記帳内容に由来しうる情報を常時第三者へ送出することになり、秘匿範囲を自分の管理下に閉じるという方針および G7 と衝突する。上限つきリングバッファは、常時記録しながらメモリ・送信量の双方に型で上限を課せる点で「膨大にならないよう工夫する」という利用者の明示要求にも直接応える。 (注意: 収集時とサーバ受信時の二重マスクを実装し、いずれか片方が漏れても秘匿値が保存されないようにすること, console のラップが開発時のスタックトレース表示を壊さないこと, バッファ長・1件あたりの文字数・総バイト数の上限を1箇所の設定として持ち、超過時は省略件数を記録すること; confidence=high; checked=2026-08-30T00:00:00Z) | opt-always-on-ring-buffer @ 2026-08-30T09:20:00Z | G5, G6, G7 |
| D9-agent-artifact-access-auth | Claude Code / Codex が改善要望のスクリーンショットと診断情報を取得する際の認証方式をどうするか | confirmed | opt-onetime-bearer-hashed:AI分析と同じ使い捨て Bearer トークン方式 (kjo_ prefix・SHA-256 ハッシュのみ保存・TTL 24h・取得回数上限) を横展開する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '既存の ai_task_tokens と同型のテーブルを1つ増やすだけ。ハッシュ化は Workers 標準の Web Crypto で行うため追加依存も費用も0。'} / free=Workers の Web Crypto API は標準機能で制限なし。D1 無料枠内。 / fit=既存 AI分析と同じ体験・同じ運用になるため、G6 の「AI分析と同様に取り出せる」を実装面でも体験面でも満たす。 / pros=既存 aiAgentRoute の実装・テスト・運用知見をそのまま流用できる, 平文トークンを保存しないため DB 漏えい時にも取得権限が渡らない, 有効期限と回数上限の二重制限で、プロンプト文字列の流出時の影響を限定できる, 取得経路が Worker に閉じ、R2 の公開URLや署名URLを一切発行しない / cons=トークンテーブルと期限切れレコードの掃除が必要, 24時間を超えて作業を中断すると再発行が要る / risks=期限切れ・回数超過を汎用の500エラーで返すと原因が分からず調査が長引くため、拒否理由を区別して返す必要がある / lock-in=低。自前実装のため外部 IdP に依存しない。 / ops=低。既存 AI分析と同じ運用に統合できる。 / evidence=https://developers.cloudflare.com/workers/runtime-apis/web-crypto/<br>opt-r2-presigned-url:R2 の署名付き URL を発行し、エージェントはその URL から直接取得する / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': 'R2 の標準機能で追加費用なし。Worker を経由しないぶんリクエスト処理も減る。'} / free=R2 Class B オペレーション無料枠 10,000,000 req/月。 / fit=画像取得という一点では最短経路だが、診断情報 (D1 側) は別方式が要るため、取得経路が2系統に分かれ G6 の単純さを損なう。 / pros=Worker を介さないため取得が高速で実装も少ない, R2 の標準機能だけで完結する / cons=署名URLは漏れた時点で期限内は無制限に再利用できる, 画像は署名URL、診断情報はBearerという二系統になり、プロンプトの記述も運用も複雑になる, 発行済みURLを個別に失効させられない / risks=プロンプト文字列ごと署名URLが第三者へ渡ると、期限まで画面スクリーンショットを自由に取得されうる / lock-in=中。署名URLの形式が R2 に依存する。 / ops=低。ただし失効手段がないぶん事故時の対応が難しい。 / evidence=https://developers.cloudflare.com/r2/pricing/<br>opt-session-cookie-only:既存のセッション Cookie 認証だけを使い、エージェント専用の認証経路を設けない / cost={'category': 'free', 'amount': 0, 'currency': 'JPY', 'billing_period': 'monthly', 'tco': '新規実装が最も少ない。'} / free=追加サービスを使わないため制限なし。 / fit=低い。Claude Code / Codex はブラウザのセッション Cookie を持たないため、プロンプトから画像を取得するという G6 の中心要件を満たせない。 / pros=認証機構を追加せず攻撃面が増えない, 実装量が最小 / cons=エージェントが取得できないため、利用者が手動で画像を保存して渡す必要がある, 自動化の価値が失われる / risks=回避策として利用者がセッション Cookie をプロンプトへ貼り付けるという、最も危険な運用を誘発しかねない / lock-in=低。 / ops=低。ただし利用者の手作業が恒常的に発生する。 / evidence=https://developers.cloudflare.com/workers/runtime-apis/web-crypto/ | opt-onetime-bearer-hashed — 既存 AI分析が同じ課題を同じ方式で既に解いており、実装・テスト・運用の知見をそのまま流用できる。ハッシュのみ保存・有効期限・取得回数上限という三重の制限は、プロンプト文字列という比較的漏れやすい経路にトークンが乗ることを前提とした設計であり、署名URL案が持つ「漏れたら期限内は無制限」という弱点を構造的に塞ぐ。画像と診断情報を同一の認証方式で扱えるため、プロンプトの記述も1系統で済む。 (注意: トークン値そのものをログへ出力しないこと, 期限切れと回数超過を区別した拒否理由を返し、汎用の500に丸めないこと, R2 オブジェクトへの公開URL・署名URLを一切発行せず、配信を Worker 経由に限定すること; confidence=high; checked=2026-08-30T00:00:00Z) | opt-onetime-bearer-hashed @ 2026-08-30T09:20:00Z | G6, G7 |
