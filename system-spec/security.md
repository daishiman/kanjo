---
status: confirmed
category: security
aggregate: 確定
spec_cells: [security.web, security.mobile, security.tablet, security.desktop-windows, security.desktop-linux, security.desktop-macos]
serves_goals: [G2, G3, G4]
---

# セキュリティ (security)

- カテゴリ集約状態: **確定**
- 章確定マーカー: `status: confirmed`

## カテゴリ別収集状態

| プラットフォーム | 状態 | 根拠 |
|---|---|---|
| Web (web) | 確定 | 確定質疑: qa-security-web-001。裏付け質疑 (`qa_refs`): `qa-duplicate-rule-001`, `qa-decision-auth-boundary-002` — 本章の「確定内容 (質疑録)」へ接地根拠として併記。資するゴール: G2, G3, G4 |
| モバイル (mobile) | 対象外 | 理由: スマートフォン向け専用アプリ (iOS/Android)を提供していたなら、本カテゴリでは端末紛失時のローカルデータ保護 (端末暗号化への依存可否・リモートワイプ) と、通信への証明書ピンニングの要否を決める必要があった。対象を web のみとする利用者決定 (qa-target-platforms-002 / appr-target-platforms-007、2026-09-05) によりその検討は発生しない。同じ関心事は 保護対象は転送中のデータと D1 上のデータに限られ、OWASP ASVS の適用範囲も web セルへ閉じる。 |
| タブレット (tablet) | 対象外 | 理由: タブレット向け専用アプリを提供していたなら、本カテゴリでは端末紛失時のローカルデータ保護 (端末暗号化への依存可否・リモートワイプ) と、通信への証明書ピンニングの要否を決める必要があった。対象を web のみとする利用者決定 (qa-target-platforms-002 / appr-target-platforms-007、2026-09-05) によりその検討は発生しない。同じ関心事は 保護対象は転送中のデータと D1 上のデータに限られ、OWASP ASVS の適用範囲も web セルへ閉じる。 |
| デスクトップ (Windows) (desktop-windows) | 対象外 | 理由: Windows 向けデスクトップアプリを提供していたなら、本カテゴリでは端末紛失時のローカルデータ保護 (端末暗号化への依存可否・リモートワイプ) と、通信への証明書ピンニングの要否を決める必要があった。対象を web のみとする利用者決定 (qa-target-platforms-002 / appr-target-platforms-007、2026-09-05) によりその検討は発生しない。同じ関心事は 保護対象は転送中のデータと D1 上のデータに限られ、OWASP ASVS の適用範囲も web セルへ閉じる。 |
| デスクトップ (Linux) (desktop-linux) | 対象外 | 理由: Linux 向けデスクトップアプリを提供していたなら、本カテゴリでは端末紛失時のローカルデータ保護 (端末暗号化への依存可否・リモートワイプ) と、通信への証明書ピンニングの要否を決める必要があった。対象を web のみとする利用者決定 (qa-target-platforms-002 / appr-target-platforms-007、2026-09-05) によりその検討は発生しない。同じ関心事は 保護対象は転送中のデータと D1 上のデータに限られ、OWASP ASVS の適用範囲も web セルへ閉じる。 |
| デスクトップ (macOS) (desktop-macos) | 対象外 | 理由: macOS 向けデスクトップアプリを提供していたなら、本カテゴリでは端末紛失時のローカルデータ保護 (端末暗号化への依存可否・リモートワイプ) と、通信への証明書ピンニングの要否を決める必要があった。対象を web のみとする利用者決定 (qa-target-platforms-002 / appr-target-platforms-007、2026-09-05) によりその検討は発生しない。同じ関心事は 保護対象は転送中のデータと D1 上のデータに限られ、OWASP ASVS の適用範囲も web セルへ閉じる。 |

## 対象外の承認範囲

> 本章の対象外セルが引用している承認の実体。状態表の「承認: <id>」だけでは、その承認が何をどこまで認めたものかを章から辿れない。

### 承認: `appr-target-platforms-007`

2026-09-05T10:05:09Z (実測 UTC)、対象プラットフォームの範囲について利用者の明示承認を得た。承認内容: 本機能の対象プラットフォームは web のみとし、モバイル・タブレット・デスクトップ (Windows/Linux/macOS) 向けの専用アプリは提供しない。これにより収集マトリクスの mobile / tablet / desktop-windows / desktop-linux / desktop-macos の5 platform × 全 8 カテゴリ = 40 セルを『対象外』として確定する。本承認を新設した理由: 従前この 40 セルは appr-foundation-total-cashflow-005 を approval_ref としていたが、-005 の note が承認しているのは『一覧表の9列常時表示』と『取込完了時点の画面警告』の2点のみで、platform 対象範囲には一切触れていなかった。approval_log の当時の全 6 件 (001/002/003/004/005/006) を全文照合しても platform 除外を承認した記録は存在せず、40 セルは承認していない承認記録を根拠にしていた (完成度 evaluator R5 の high 所見、および独立マトリクス監査が自ら PASS を撤回して到達した同一結論)。参照先の付け替えでは解けない欠陥であり、実際に利用者へ問うことでのみ閉じられるものであったため、AskUserQuestion で代替案 B・C を併記して確認を取った。質疑の実体は qa-target-platforms-002 (basis=user-decision) に逐語で記録してある。本承認の範囲はこの platform 対象範囲の1点に限り、U1-U9 全文の承認ではない。

## 上流指針 (doctrine anchors)

> 本章の設計判断が従う上流の正本 (1 concern 1 authority)。具体技術ではなく上流工程を導く規範であり、下位の技術選定は本節と矛盾してはならない。正本: `ref-system-design-knowledge/references/doctrine-anchor-registry.json`

| 設計 concern | 上流の正本 (authority) | 導く範囲 | 出典 | 最終確認 | 本章の確定セルへの反映 |
|---|---|---|---|---|---|
| security | OWASP ASVS + Secrets Management Cheat Sheet | 脅威モデル・入力検証・暗号化・監査ログの上流指針 | https://owasp.org/www-project-application-security-verification-standard/ | 2026-07-12 | 本章は脅威を 4 つ (S1 未認証閲覧 / S2 ブルートフォース / S3 canonical 破壊 / S4 不正入力によるデータ汚染) に特定し、それぞれへ対策を確定した。上流指針が効いているのは特に S3 と S4 である。S3 に対し本機能を『読み取り専用の投影』と定義し、書込面を DuplicateVerdict の upsert 1 点に限定したのは、完全性 (税務の正本である freee 帳簿を壊さない) を機密性と並ぶ最重要属性に置いた結果で、既存の canonical-mutation-fence と同じ分離方針へ揃えている。S4 に対する zod スキーマ検証と明細識別子の MfTx.idStable への限定は、入力検証を境界で行うという上流の要求の具体化。暗号化については、保存する新規データが利用者自身の判断フラグのみで新たな秘匿情報を増やさないため、追加の暗号化要件は設けていない。 |

## 確定内容 (質疑録)

> 本章の各確定セルが何を根拠に確定したかの実体。`qa_ref` が主たる接地根拠、`qa_refs` がそれを支える裏付け質疑であり、いずれも qa_log (spec-state.json) の逐語である。ここに現れない主張は本章の確定内容ではない。

### Web (web)

- 資するゴール: G2, G3, G4

#### 主たる接地根拠: `qa-security-web-001`

**問**

この機能の脅威モデルと主要な対策は何か。

**答**

扱うのは利用者本人の事業帳簿と家計の金額データであり、機密性(第三者に見られない)と完全性(税務の正本である freee 帳簿を壊さない)を最重要とする。脅威と対策: (S1)未認証の第三者による財務データ閲覧 → run_worker_first による /api 全面の認証ゲート、Cloudflare Access JWT 検証またはHMAC署名 HttpOnly Cookie、タイミング非依存比較(SHA-256ダイジェスト同士の比較)で対処。(S2)パスワードへのブルートフォース → 既存 login-rate-limit.ts の窓・上限・ロックで対処。(S3)本機能が canonical データを破壊すること → 本機能は読み取り専用の投影として実装し canonical を書き換えない。書込面は DuplicateVerdict の upsert 1点のみに限定し、既存の canonical-mutation-fence と同じ方針で正本更新経路と分離する。(S4)不正・不整合な入力によるデータ汚染 → 追加APIの入出力を zod スキーマで検証し、明細識別子は MfTx.idStable(安定識別子)に限定して不安定な識別子での判断保存を拒否する。(S5)ログ・エラー応答からの金額や取引先の漏洩 → observability は有効だが、追加する処理でリクエストボディや明細内容をログへ出さない。(S6)誤った重複判定による金額の過小計上 → 事業費へ寄せるのは freee 側の件数を上限(min(n,m))とし、freee に存在しない分の MF 明細を消さない不変条件を契約テストで固定する。監査証跡は既存 audit-log.ts の方針に従い、DuplicateVerdict の変更を追跡可能にする。

- (根拠の性質: コード・設定・公式文書で検証できる観測事実 / 出所: アシスタントによる既存リポジトリの読解 (wrangler.jsonc / packages/{api,core,web} / migrations) に基づく現状記述。利用者への個別確認は経ておらず、誤りがあればコードとの突合で検出される種類の回答である。)

#### 裏付け質疑: `qa-duplicate-rule-001`

**問**

重複した明細をどう扱うか。照合キーは何にするか。同じキーに MF と freee で件数が食い違う場合 (n≠m、とくに freee 側が多い m>n) はどうするか。

**答**

利用者回答: 金額と日付がイコールのものについては freee を正とし、Money Forward 側を削除(集計から除外)する。日付は Money Forward の日付と freee の発生日 (FreeeDeal.date) を突き合わせる。金額と日付が一致するものは基本的に『事業で使う費用』として扱う。それ以外の費用は家計の方につける。ここから導いた基数規則: 同じ(金額, 発生日)のキーに MF が n 件・freee が m 件あるとき、MF から min(n,m) 件を家計費から外して事業費へ寄せる。(i) n>m のとき、MF に freee より多く明細がある = freee に載っていない支出が (n−m) 件あるということなので、その分は家計費に残す。三井住友の残り2口座の支出が消えないのはこの規則による。(ii) m>n のとき、freee 側に MF に現れない事業経費が (m−n) 件ある = 事業口座から直接払った経費であり、MF 側に消すべきものが無いので MF からは n 件しか外さない。いずれの向きでも『freee にない MF 明細を消さない』『MF にない freee 明細を二重に数えない』が保たれる。金額が同一のキー内では、どの MF 明細を外しても合計は変わらないため、外す対象の選び方は合計値に影響しない。既知の限界として、支払先を判定に用いないため偶然に同額・同発生日となった無関係な支出も事業費として扱われうる。利用者はこれを承知のうえで、規則の単純さと予測可能性を優先すると判断した。また freee の dueDate / settledDate は判定に用いない(カード引落日で照合する取り違えを防ぐ)。日付が一致しない重複(カード利用日と freee 発生日のズレ)は自動判定の対象外となり、要確認として可視化する。

- (根拠の性質: 利用者が代替案を見たうえで明示選択した決定 / 出所: 利用者の自由記述発話 (二重計上の扱い・freee を正とする指示・収入は MF の事業副業を事業収入とする指示) を根拠に、アシスタントが規則へ整理した回答。選択肢提示は無く、利用者が明示的に選んだのは規則の骨子であって整理後の文面ではない。)

#### 裏付け質疑: `qa-decision-auth-boundary-002`

**問**

【AskUserQuestion / 2026-09-04】本機能 (トータル収支の一覧表と要確認判断API) の認証境界をどこに置くか。3案を推奨理由・注意点付きで提示した。(A) 既存の /api 認証ゲートの内側に置く〔推奨〕— 認証境界が1つに保たれ、wrangler.jsonc の run_worker_first: ['/api/*'] により追加APIも必ずゲートを通る。実装・運用の増分ゼロ。ただし将来 税理士など第三者へ一覧表だけ見せたくなった場合は決定の見直しが要る。(B) 読み取り専用トークンを発行し一覧表だけ別経路で公開する — 第三者共有には対応しやすいが、守る認証境界が2つになり、トークンの失効・ローテーション漏れが財務データの露出へ直結する。(C) Cloudflare Access へ一本化しアプリ内セッション認証を廃止する — 本番の防御は強いが、ACCESS_AUD/ACCESS_TEAM_DOMAIN 未設定のローカル開発・プレビューでは機能せず、開発と本番で認証経路が分岐する。

**答**

利用者回答 (AskUserQuestion の選択肢から明示選択): 「既存の /api 認証ゲートの内側に置く (推奨)」を選択。これにより意思決定 dec-auth-boundary-001 の user_decision を option_id=opt-existing-gate / confirmed_at=2026-09-04T14:52:00Z として確定した。棄却案は (B) 読み取り専用トークンで別経路、(C) Cloudflare Access へ一本化。本エントリは、前サイクルの qa-auth-web-001 が『本機能は新しい認証境界を作るか』という代替案を示さない Yes/No 誘導であったという独立ヒアリング監査の指摘を受け、代替案を明示したうえで採択を取り直した記録である。認可はロール分割を持たない単一主体モデルのままとし、複数人利用が始まる場合はこの前提が崩れることを注意点として提示済み。

> **訂正あり** — 直上の答は凍結された記録であり、後から次の訂正が入っている。
> 本文中の記述と食い違う場合は、訂正側が正である。
>
> - `2026-09-05T01:59:27Z` — 直上の答の本文に書かれた confirmed_at 2026-09-04T14:52:00Z は誤りである。当該時刻は spec-state.json への実書込時刻 2026-09-04T14:33:18Z より後であり、その時点では発生し得ない値だった (完成度 evaluator が『時刻の捏造』として検出)。正しい値は decisions[].user_decision.confirmed_at の 2026-09-04T21:27:59Z で、これは採択時刻そのものではなく『遅くともこの時刻までに利用者採択が済んでいる』ことを示す上界である。利用者が選択肢から明示選択したという事実そのものは変わらない。答の本文は改竄防止のため凍結されており書き換えられないため、訂正をここに残す。
> - `2026-09-05T02:12:49Z` — この答の本文が述べる時刻には二重の誤りがあり、直前の訂正もまた誤っていた。(1) 本文の 2026-09-04T14:52:00Z は実書込時刻 14:33:18Z より後で発生し得ない値だった。(2) それを訂正した 2026-09-04T21:27:59Z は『訂正を書き込んだ時刻』を上界として充てたもので、採択時刻ではなかった。そのとき私は『選択の正確な時刻は記録していなかった』と書いたが、これも誤りである — AskUserQuestion の応答は会話トランスクリプトへ時刻付きで残っており、探せば読めた。上界で代用する必要は最初から無かった。(3) 独立ヒアリング監査 (2026-09-05) の指摘 F2 を受けてトランスクリプトを照合し、実測値を復元した — 質問提示 2026-09-04T13:59:32Z、利用者回答 2026-09-04T14:00:13Z。現在の decisions[].user_decision.confirmed_at はこの実測値である。なお本件・認証境界・集計方式の3決定は同一の AskUserQuestion 呼出しで同時に提示・回答されており、3件の confirmed_at が一致するのは書込時刻の使い回しではなく、同一の利用者ターンだったことの反映である。

- (根拠の性質: 利用者が代替案を見たうえで明示選択した決定 / 出所: AskUserQuestion による利用者への直接質問。代替案を併記したうえで利用者が明示選択した。)

## To-Be / Delta

> 本章の**規範**。上位概念 (要件定義書 U3 ゴール / U4 目標 / U9 具体的やりたいこと) を本章の serves_goals で絞り込んだ射影であり、設計知識 card (非規範の参考資料) とは役割が異なる。As-Is (現行実装の姿) は spec-state.json の管轄外のため本節では断定せず、到達点と、その到達を判定する観測点だけを規範として置く。

### 到達すべき状態 (To-Be)

- **G2**: Money Forward の日付と freee の発生日が一致し金額も一致する支出は『事業で使う費用』として freee を正に事業側へ一度だけ計上し、それ以外の支出は家計側へ計上して、二重計上のない支出合計を出す状態にする
- **G3**: 事業側へ寄せた金額と件数を一覧表の中に明示し、合計が『事業費(freee正) + 家計費(MF残余)』として利用者の手で検算できる状態にする
- **G4**: 金額または発生日が一致せず自動で事業費へ寄せられなかった重複候補を『要確認』として理由付きで列挙し、利用者が一度『同じ/違う』を判断すれば、次回以降の取込でその判断が再適用される状態にする

### 受入条件 (Delta の判定点)

| 目標 | 到達点 | 達成の観測点 (measure) |
|---|---|---|
| O1 | 月次のトータル収支行を導出する読み取りモデルを core に追加する | months の各月について 総収入・総支出・総収支・事業費・家計費・事業費へ寄せた件数 を返す関数が存在し、契約テストで恒等式 総支出 = 事業費 + 家計費 と 総収入 = 事業収入 + 家計収入 がいずれも全月で成立する |
| O2 | 『MF日付 = freee発生日 かつ 金額一致なら事業費、それ以外は家計費』という支出帰属規則を単一の実装として持つ | (金額,発生日)ごとに MF n 件・freee m 件のとき MF から min(n,m) 件が事業費側へ寄ることを契約テストで固定し、freee 件数を超える付替が0件、支払先を用いた判定が0件、日付近傍(±3日)の照合が要確認候補の抽出にのみ使われ自動付替を1件も行わないこと、および利用者の『同じ』判断による付替が freee 側1取引につき MF 1件までであることを示す |
| O3 | 収入の帰属を MF の事業・副業区分で決め、freee 売上との二重計上を避ける | MF の事業・副業入金が事業収入へ、それ以外が家計収入へ計上されることを契約テストで固定し、freee 売上と同額・同発生日で重なる分が総収入へ二度加算されないことを示す。加えて、日付が一致せず消し込めない組が要確認として出ること、および利用者が『同じ』と判断した入金が家計収入から外れて事業収入へ寄り、判断の前後いずれでも総収入 = 事業収入 + 家計収入 の恒等式が成立することを固定する |
| O4 | 要確認明細の判断を永続化し、集計へ反映したうえで再適用する | 利用者が下した『同じ/違う』の判断が保存され、再取込後も同じ明細へ再適用されることを結合テストで示す。加えて『同じ』判断が付いた明細が事業費側へ寄り、判断の前後いずれでも 総支出 = 事業費 + 家計費 の恒等式が成立することを契約テストで固定する |

### 本章がかなえる具体的やりたいこと (U9)

- **I2**: 一覧表に『事業費へ寄せた額』『その件数』の列を置き、支出合計の内訳(事業費/家計費)を見せる
- **I3**: MF 明細の日付と freee 取引の発生日が一致し金額も一致する支出は事業で使う費用とみなし、freee を正として事業費に一度だけ計上し MF 側を家計費から外す。同じ(金額,発生日)に MF が n 件・freee が m 件あるときは MF から min(n,m) 件を家計費から外す(金額が同一のため、どれを外しても合計は変わらない)
- **I4**: MF 側で事業・副業に分類される入金を事業収入として扱い、それ以外の入金を家計収入とする。freee 売上と同額・同発生日で重なる分は支出と同じ規則で freee を正とし二重計上を避ける。日付が一致せず消し込めない組は要確認として出す
- **I5**: 自動で確定できなかった重複候補を、理由付きの要確認一覧として出す。候補は『金額一致 かつ 発生日が±3日以内』で抽出し、この抽出自体は合計を変えない
- **I6**: 要確認明細への『同じ/違う』判断を既存の取込明細編集画面から行えるようにし、判断を保存して次の取込でも再適用する。『同じ』判断は合計へ反映する — 支出は当該 MF 明細を家計費から外して事業費へ寄せ、収入は当該 MF 入金を家計収入から外して事業収入へ寄せる (支出と収入で同一の規則)

### 本章に効く確定意思決定

- **dec-aggregation-strategy-001**: 月次トータル収支 (総収入・総支出・総収支・事業費・家計費・寄せた件数) をどう算出するか。要求時に毎回導出するか、事前計算して保存するか、SQL 側へ寄せるか。
  - 採択: 要求時に canonical から毎回導出する (`opt-derive-on-request`)
  - 目的適合: G3 (合計が『事業費 + 家計費』として利用者の手で検算できる) を最も直接に満たす。canonical が唯一の真実であり、画面の合計と明細が定義上ずれない。G7 (期間切替でも矛盾しない) も、期間ごとに同じ導出関数を通すだけで満たされる。
  - 採択の接地根拠: `qa-decision-aggregation-002`

## 適用された設計知識

> 以下の deep knowledge card は設計判断を支援する**非規範の参考資料**であり、実装済み・検証済みの証拠ではない。カード内の `採否: applied` は設計採用を意味し、実装状態は意味しない。規範となる差分は本章の To-Be / Delta 節と参照先仕様で管理する。

### 本章での適用

secure-by-design の『攻撃面は列挙できる大きさに保つ』を、本機能が増やす入口の評価に適用した。本章が確定させた攻撃面は /api と CSV 取込経路の 2 つだけで、今回の追加はどちらの数も増やさない — 一覧表は既存 /api 配下の読取、二重計上の判定は取込済みデータに対する内部処理である。むしろ本機能で増えるのは入口ではなく『判断の永続化』という新しい状態であり、ここが security 上の実質的な新規面である。利用者の『同じ/違う』判断は集計の値を動かすため、判断の書込は明細の書込と同じ認証ゲートを通し、取込由来のデータが判断を上書きできないようにする。CSV は外部サービスの出力であって信頼済み入力ではないため、金額・日付は取込時に検証済みの型へ落としたものだけを照合に用いる。

- (根拠の性質: アシスタントの推定 (利用者確認も検証可能な出典も経ていない) / 記録時刻: 2026-09-05T02:02:16Z)

### Secure by Design — deep knowledge card

- 出典カード: `ref-system-design-knowledge/references/secure-by-design.md`

#### 目的

利用者の注意や運用後のpatchへ安全性を押し付けず、systemのdefault、architecture、development lifecycleに安全な結果を組み込み、被害可能性と復旧費を下げる。

#### 解決する問題

- 認証・認可・data protectionが後付けで、business flowと矛盾する。
- defaultが過大権限/公開状態で、利用者の完全な設定に安全性が依存する。
- 単一防御の突破で全面侵害になり、検知・封じ込め・復旧の証拠が無い。
- dependency、secret、build、releaseの供給chain riskが製品境界外として放置される。

#### 適用条件

- identity、個人/機密data、金銭、外部入力、admin操作、multi-tenant boundaryを扱う全system。
- compromise時の影響がgoal、法規、信頼、運用継続を損なう。
- vendor/serviceを使う場合も、共有責任とfailure/exit planを明示できる。

#### 非適用条件

- security自体が不要なsystemは原則ない。asset/threatが極小ならcontrolを軽量化できるが、根拠付きrisk acceptanceが必要。
- controlがthreatを減らさず、accessibility/availability/safetyを重大に損なう場合はそのcontrolを採用しない。代替・補償統制を設計する。
- checklist準拠だけでproject固有のtrust boundaryとabuse caseを置き換えない。

#### トレードオフ・失敗モード

- friction、latency、delivery費、運用負荷が増えるため、risk reductionと明示的に釣り合わせる。
- security theaterとしてcontrol数だけ増やし、owner、evidence、responseを持たない。
- fail closedを無差別適用してavailability/safety incidentを起こす。degraded modeとbreak-glass監査が必要。
- secretを隠しても過大権限や長期credentialを残す、暗号化してもkey lifecycleを設計しない等の局所最適。
- free tier製品を価格だけで選び、audit、export、retention、MFA、incident support不足を見落とす。

#### goalへの寄与

- stakeholderの安全・信頼・継続性をsuccess criteriaへ変換し、threat/control/evidenceをgoalへトレースする。
- security controlは「導入済み」ではなく、阻止/検知/復旧時間、権限範囲、data exposureで効果を測る。
- 予算0制約でも、secure default、最小data、短命credential、標準機能、open-source検査を優先し、残余riskを隠さない。

## 最新ドキュメント出典

- (このカテゴリに割り当てた取得済みドキュメントなし。全体出典は index.md 参照)
