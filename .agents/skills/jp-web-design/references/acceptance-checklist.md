# 検収チェックリスト

UI実装の検収に使う全項目。実装完了の判定に使う。体験面(デフォルト・一括操作・正直なUI・エラー回復)の検収は Skill `ux-design` のチェックリストを併用する。

- [ ] **情報設計の工程を通したか**(`information-design.md` §2): 使われる場面の1文がある / ラベルを一度外し、伝わらないものだけ最小限に補った / 情報は原則3〜4群に束ね余白で分けた / 超過時はその方が判断・入力を早くする理由をT2または同等正本に残した / 群内に主役が1つある。
- [ ] **表示形式を6判断軸から導出したか**(同 §5-1〜5-2): 選定理由と却下した候補が1行で言えるか。§5-6 の検証(0件/1件/最大件数・文字量2倍・4幅実測・「この形式でないと何が遅くなるか」)を通したか。
- [ ] 通常の表示形式・ラベル・加工・画像の判断は標準の `docs/product/T2-experience-spec.md`(またはプロジェクトの同等正本)に記録したか。前例のない例外の場合だけ、§9 の手順で導出し `docs/product/design-decisions.md` に分離したか。
- [ ] DBの生値(ISO日時・コード値・真偽値・内部ID・生年月日)がそのまま出ていないか。加工値から正確な値へ到達できるか(突合・出力・入力欄は生値が正)。
- [ ] 画像ごとに識別/証拠/説明/装飾の役割を決め、装飾だけの画像は原則削除したか。採用した画像は目的に合う alt・必要なキャプション・焦点を守るトリミングを持ち、375/768/1280/1600pxの4幅で重要部分の欠落・極小文字・レイアウトシフトがないか。
- [ ] 装飾が「押せる/選択中/重要/隠れ機能」のいずれかを伝えているか。ゼブラ・表ヘッダーのベタ塗り・分類ごとの色分けなど彩り目的の装飾が残っていないか。
- [ ] Mode AはGraphite × Amberの役割契約か。primary=CTA/操作/選択、accent=実行中だけになっており、主要CTA・キー数字・AI装飾へaccentを流用していないか。
- [ ] Light/Dark/autoが正しく動き、手動指定がOS設定より優先され、選択が永続化されるか。Darkでpage-bg/bg/surface/surface-alt/borderの明度差が保たれているか。
- [ ] success/warning/danger/neutral/runningが色+文言+borderで区別され、accentとwarningを色だけで見分けさせていないか。
- [ ] AI機能へ紫・ネオン・専用グラデーション・専用コンポーネントを与えず、通常機能と同じ設計言語になっているか。
- [ ] 説明文の連なりを視覚要素(数字・サイズ差・チップ・面)に置き換えたか。文字文字しい画面が残っていないか。
- [ ] 「濃く太い数字=変わる値」の文法が全画面で統一されているか(値/ラベル/単位の描き分け)。
- [ ] 1画面に視覚的主役が1つ決まっており、主役:本文のサイズ比が2.5倍以上あるか。同格要素のサイズは揃っているか。
- [ ] 初見でマニュアルなしに最頻タスクが完了できるか。押せないものに押せる見た目が付いていないか。
- [ ] ヘッダーのアプリ名クリックでホームに戻るか。
- [ ] **375 / 768 / 1280 / 1600px の4幅を実測**し、「1文字だけ改行」「1個だけ折返し」「途中折返し」「ワイドの間延び」がないか。
- [ ] ナビ骨格を項目数・最頻操作から選んだか。サイドバー→アイコンレール→下部タブを使う場合、640/1024px、最大5項目、safe-area、本文下padding、`aria-current`を実測したか。
- [ ] `word-break: break-all` を広い範囲に当てていないか。短いラベル・ボタン文言・表ヘッダーが折り返していないか(収まらないものは文言短縮か省略表示になっているか)。単位・記号が行頭行末に孤立していないか。
- [ ] 現在地(ステップ・タブ)と主要アクション(保存・戻る・次へ)が固定表示されているか。共通レイアウト部品として1箇所に実装されているか。縦幅が狭い環境で本文が潰れず、Tab移動でフォーカスが固定要素の下に隠れないか。
- [ ] SPで情報削減(カード化・折りたたみ・上位N件)が入っているか。全情報を縮小して詰め込んでいないか。
- [ ] テーブルの数値は「生数値+カンマ・右揃え・単位は列ヘッダー・1列1単位」か。数字が和文フォントで描画されていないか(`--font-num`)。主数字のカンマは縮小(`.num-sep`)・通貨記号は単位扱い(`.currency`)か。
- [ ] 12px未満の本文・補足がないか。補足文は1行以内か(長い説明はplaceholder・ラベル・開示へ移したか)。
- [ ] 不可逆な送信がEnter即送信になっていないか。ショートカットはキーキャップUIで表示されているか。
- [ ] 使い回す部品の切替がコンテナクエリで書かれているか(部品にメディアクエリを直書きしていないか)。
- [ ] バーチャートは `components.md` の3条件を満たすものだけか。比率を恣意スケールで描いていないか。
- [ ] カード左端の色帯を使っていないか(禁止。分類色はチップ/ドット/バーで)。
- [ ] フォーム: ラベル上置き・エラーが項目の場所で出る・必須が視覚+テキストで分かるか。
- [ ] すべての部品に hover / focus-visible / disabled / loading / error / empty があるか。
- [ ] hoverは操作可能な要素だけにあり、border/surface中心・90〜140ms・移動1px以下か。タッチでhoverなしでも重要情報と操作に到達できるか。
- [ ] 入場は追加対象だけ・6要素以内・全体300ms以内か。popover/modal/accordion/toast/状態更新の動きに因果があり、再レンダーごとに再生されないか。
- [ ] モーダルにフォーカストラップ、エラートーストに次のアクションがあるか。すべての装飾アニメーションが reduced-motion で止まり、意味と操作が残るか。
- [ ] `prefers-contrast: more` / forced colors / axe等のコントラスト検査をLight/Darkで確認したか。特にDark warningがAAを満たすか。
- [ ] **動的パスを実際に操作して検証したか**: 選択→件数連動 / モーダル開閉(ESC・フォーカス復帰) / 実行中→部分成功→再試行 / 下書き保存→復元→破棄 / Enter・⌘+Enter。静的スクショだけで検収しない。
- [ ] 比喩コピー・多色アイコン・絵文字が混入していないか。表示している数字はすべて本物の計算結果か。
